# (Right! Luxury!) Lakehouse — TODO

Quick-reference action items. Full details in [ARCHITECTURE.md](ARCHITECTURE.md). For research directions and unscheduled ideas, see [ROADMAP.md](ROADMAP.md).

**Last updated**: 2026-04-25 (Kimball PR 5b player_key embedding marts merged [#202]; ExT v2 reproduction design spike added as D66 with paired ROADMAP "ExT-style Conditional xT" section)
**(A) Kimball Migration through PR 5a shipped** — see §Kimball Migration below. PR #190 (2026-04-24): 4-provider `dim_teams` + `dim_players` with BIGINT surrogates, entity resolution active across StatsBomb/Wyscout/IDSSE/Metrica (3,977 player-xref + 152 team-xref pairs), Metrica pseudo-competition, Wyscout teams.json ingestion (142 teams), fct_match_summary/player_stats/funnel migrations. Both PR 4b warn-suppressions closed (7,587 Wyscout opponents + 1 NULL outlier dropped). Plus 5 same-cycle hotfixes deployed: #191 CI freshness pin-preserve, #192 Heat Map Top-N restore + initial dependabot ignore, #198 root-cause fix (stop committing `hf_taipy_app/requirements.txt`), #196 + #199 legit dev-dep updates, #200 competition dropdown dedupe.
**(B) ScoutGPT cross_attention promotion cycle** in flight on branch `evolve/scoutgpt-cross-attn-promote` — pre-registered rules fired PROMOTE + KEEP: Arm 1 additive rho=0.0534, Arm 5 cross_attention rho=0.3003, Arm 2 fourier rho=0.2963. Default flipped `additive → cross_attention` in `ScoutGPTConfig`; Fourier kept as enum alternative. Wheel 0.3.10 → 0.3.11. Media-PC validated as SSH-reachable training host (→ EV3 partial). Awaits commit.

---

## On Deck

Tasks warming up in the on-deck circle.

| Size | What it means |
|------|---------------|
| **Monstah** | Multi-phase epic — clear the wall or go home |
| **Wicked** | Looks small, surprisingly impactful |
| **Dunkin'** | Quick run, keeps things moving |

| # | Task | Size | Source | Notes |
|---|------|------|--------|-------|
| G2G3 | SDK synced-table path hardening (G2 + G3 from Kimball PR 4b, 2026-04-23) | Wicked | PR 4b deferral with explicit user approval | **Blocking condition:** any future PR that switches synced-table creation from Terraform/UI to `w.postgres.synced_tables.*` SDK path MUST close both gaps before shipping. **G2 (confirmed real):** `src/ingestion/refresh_synced_tables.py:178` hits `/api/2.0/database/synced_tables/` (legacy REST endpoint). SDK-created synced tables live under `/api/2.0/postgres/synced_tables/`. An SDK-created table is NOT addressable by the current refresh module. **G3 (unverified):** `scripts/run_lakebase_grants.py` + `scripts/fix_event_log_ownership.py` behavior post-SDK-create — unclear whether ADR-005 grants flow and event_log ownership semantics hold on the new creation path. Needs empirical verification via an SDK-create + grants run + event_log ownership check cycle. **Preparatory work already landed in PR 4b:** G1 `wait_until_online(table_fqn, ...)` helper in `src/ingestion/refresh_synced_tables.py` polls `status.detailed_state` for `SYNCED_TABLE_ONLINE_NO_PENDING_UPDATE`. Reusable by whichever PR closes G2 + G3. **References:** `docs/superpowers/specs/2026-04-23-kimball-pr4-action-values-plus-deferrals-design.md` §2 + §6.7; memory `project_sdk_synced_table_gaps_for_pr4.md`. |
| D40d | `for_each_task` Fan-Out for Pitch Control + Off-Ball xT + long ingests | Wicked | D40 follow-up, extended 2026-04-21 with PR 1.8 learnings | Guards compute `FilterResult.chunks` (2 matches/chunk) but Terraform does not wire `for_each_task`. **Steps:** (1) Monitor per-task runtimes from `system.lakeflow.job_task_run_timeline`, (2) verify `for_each_task` Terraform syntax, (3) wire fan-out for `compute_pitch_control` and `compute_off_ball_xt` — each iteration receives a chunk from the guard's `FilterResult.chunks`, (4) test with a manual job run. Respo.Vision will require fan-out from day one when data arrives. **Extension (2026-04-21, PR 1.8 cycle):** same fan-out pattern also resolves the new `ingest_idsse` / `ingest_skillcorner` 15-min timeout pain. PR 1.8's wide parsers (+14 cols on IDSSE tracking, finalize_bronze_df guard) doubled per-match wall-clock, so one full 7-match sequential run (~45 min) no longer fits the 15-min task budget — required 5 sequential job re-triggers to land 7/7 matches on 2026-04-21. Alternative ("Terraform timeout bump to 2700s") is a band-aid that preserves serial execution; the `for_each_task` fan-out fix unlocks all the parallelism Serverless already pays for (each per-match sub-task finishes in ~7 min comfortably inside 15-min). **Add steps (5)** extend `ingest_idsse` / `ingest_skillcorner` / `ingest_metrica` entry points to accept a `--match-ids` or `--chunk-index` CLI arg so they can consume the fan-out's per-iteration chunk, **(6)** verify with a force re-ingest (DELETE bronze rows first) that all matches land inside the 15-min budget per chunk. |
| D35 | AI/ML Workflows — Detail Drilldown & Card Validation | Wicked | [2026-03-23-taipy-workflows-page.md](docs/superpowers/plans/2026-03-23-taipy-workflows-page.md) | Enable the 8-section detail drilldown panel (designed but disabled — UX design not settled). Sections: overview, data flow, execution config, monitoring, cost breakdown, academic provenance, dependencies, changelog. Requires design decisions on navigation (slide-in panel vs sub-page vs modal) and information density. Also: validate all 16 workflow card YAML files against reality — the drilldown makes card data visible for the first time, so expect corrections to estimates, dependencies, and monitoring thresholds |
| D33 | ScoutGPT Integration — Embeddings, pgvector & Taipy | Wicked | [adversarial-training.md](docs/research/adversarial-training.md) | Extract player embeddings from trained ScoutGPT model. Write to Delta via new `fct_player_embeddings_sequence` mart (or extend existing). Synced table + pgvector HNSW index. Add model selector to Player Similarity Taipy page ("Football2vec" vs "ScoutGPT"). Counterfactual substitution UI: "what would Player X do in Team Y's possessions?" Side-by-side comparison dashboard between old and new embeddings. Follows D32 (complete) |
| EV3 | Evolve Engine — Wire RemoteSSHBackend into ScoutGPT evolve target | Dunkin' | Session 43 (2026-04-16); EV2 cycle shipped ~75% as side-effect (2026-04-24) | **Status (2026-04-25):** `src/evolve/backends/remote_ssh.py`, `BackendPool` priority-queue integration, and Media-PC `super@192.168.68.70` validation all shipped as EV2 cycle side-effects (see `docs/engineering/orchestration.md`). **Remaining scope:** wire the existing `RemoteSSHBackend` into the ScoutGPT evolve target's evaluator — currently the ScoutGPT evolve target dispatches only to `LocalCudaBackend` / HF Jobs, not to remote SSH hosts. Enables 2x local GPU parallelism *for ScoutGPT cycles* (already proven for Football2Vec via EV2 multi-backend dispatch). Environment prerequisites in `memory/user_local_hardware.md`; setup-gotchas in `memory/feedback_orchestration_lessons.md` rules 6-10. Effort: ~half-day given infrastructure now exists. |
| D7 | Observability Layer (OTel) | Monstah | [ROADMAP.md](ROADMAP.md) | Research complete, ready for implementation. Instrument once, observe anywhere. ~$1-2/month personal tier |
| U3 | Global player search — search by name across all pages | Monstah | CHI-AUDIT-180-rev-1 #1 | New search component with 11,918-player index + cross-page routing + session state. Needs design decisions |
| U4 | Uncertainty/confidence bounds on model outputs | Monstah | CHI-AUDIT-180-rev-1 #4 | xG v2 now outputs MC dropout 95% CI (`xg_ci_lower`, `xg_ci_upper`). VAEP/pitch control still lack native uncertainty. Partial — xG done, others remain |
| PA2 | Set Piece Effectiveness Page | Wicked | Performance analysis courses (all 5) | Zero set piece analysis across 16 pages, yet set pieces account for ~25% of WC 2022 goals (Power et al., STATS AI Group). New page: corner delivery analysis (in-swing 2.7% vs out-swing 2.2%, flick-on 4.8% vs direct 2.0%), FK crosses vs direct shots (1.1% vs 7.2%), crossing zone heatmap (Gelade 2017 OptaPro: Zone 4 cutbacks 7.2% vs Zone 1 deep crosses 0.2%), throw-in outcomes by zone. StatsBomb event data has set piece type, delivery zone, and outcome metadata. **Template:** `PageConfig` with sidebar filters (set piece type, side, competition) + content blocks (zone heatmap, conversion funnel, outcome breakdown). |
| PA3 | Throw-In Analytics Page | Wicked | Performance analysis courses (L3, Gronnemark Throw-In Academy) | 40-60 throw-ins per match, 15-20 min of play — currently invisible in the platform. Gronnemark's coaching produced +23 percentage point improvement at Liverpool (45.4% → 68.4% possession retention under pressure, EPL rank #18 → #1). **Metrics from StatsBomb events:** per-zone count (defending/middle/attacking third), possession retention rate (sequence analysis: did team retain after throw?), fast throw-in % (time from ball out to throw, where available), throw-in direction (forward/backward/lateral), throw-to-chance/goal sequences. **Tracking data extension (where available):** effective range, opponent defending pattern classification, space created pre-throw. Unique platform differentiator — very few analytics tools cover throw-ins at all. |
| PA1 | Game State Segmentation — App-Side Filter | Wicked | Descoped from initial PA1+PA4 (2026-04-10) | **dbt layer complete:** `game_state` column in `fct_shots`, `fct_passes`, `fct_action_values` via `int_running_score`. Index `idx_action_values_comp_team_gs` exists. **App layer descoped:** shared `selected_game_state` dropdown was removed from all non-funnel pages after destabilizing filter propagation. Game state is currently local to Conversion Funnel only. **To re-enable:** (1) validate query performance with `EXPLAIN ANALYZE` on each page's query with `WHERE game_state = %s` — the funnel debacle proved blind query changes waste hours, (2) add `game_state` indexes to `fct_passes_synced` and `fct_shots_synced` if needed, (3) re-add `game_state` param to query functions (`shots.py`, `passes.py`, `tracking.py`, `defensive.py`, `goalkeepers.py`), (4) re-add shared state variable + callback + sidebar widget, (5) Puppeteer-verify filter propagation on all pages after wiring. |
| U6 | VAEP/xT three-axis UX labeling | Dunkin' | xR paper (2026-04-14) | **Goal:** decompose VAEP/xT displays in the Taipy dashboard into three labeled cognitive axes — **Survival** (did the action keep the ball), **Progression** (was the state advanced if successful), **Decision Value** (was the implicit risk worth it given context) — without touching the underlying models. **Source:** García de Marina, "xR: Expected Retention as a Foundational Metric for Possession Survival in Football" (SOCCHUB, 2026). We borrow the *pedagogical framing only*; the xR model itself is NOT implemented because tracking data is insufficient (~17-20 matches total, below the threshold for a context-conditional retention model). **Changes:** (1) update `help_text` tooltips on VAEP/xT `Metric` entries across player-comparison and action-value pages to name the three axes with scale and direction, (2) add `Survival`, `Progression`, `Decision Value` entries to `GLOSSARY` in `hf_taipy_app/src/template.py` and reference them in each affected page's `PAGE_TERMS`, (3) add a "Three-axis interpretation" prose section to `docs/huggingface/model-cards/vaep-model.md` noting the framing is borrowed but the VAEP model is unchanged. **Stretch (+1 day):** 2D scatter "Style panel" on the player profile page — x = progression index, y = retention index, dot size = sample size; uses values already in mart tables (no new inference). **Out of scope:** no new Databricks workflows, no new mart tables, no xR model training, no composite-score computation, no changes to `compute_spadl_vaep` or `compute_expected_threat`. **Citation posture:** do NOT add a formal `Citation(text, url)` to any `PageConfig` or `references:` entry in any workflow card — the paper is a SOCCHUB blog post with methodologically soft rigor, and triggering the Appendix D academic-reference audit (`test_architecture_md_appendix.py`) against a non-peer-reviewed source is not justified. Cite only in the VAEP model card prose. **Acceptance:** every VAEP value on every Taipy page has a three-axis tooltip or caption; `GLOSSARY` is populated; VAEP model card documents the framing; `mad-scientist-skills:cognitive-interface-audit` run before merge validates scale/direction compliance. |
| U7 | Match Summary verdict vocabulary expansion | Dunkin' | [2026-04-19-match-summary-redesign-design.md](docs/superpowers/specs/2026-04-19-match-summary-redesign-design.md) §15 | Extend the v1 5-phrase verdict set (`Fully merited` / `Fair result` / `Fortunate` / `Smash & grab` / `Flattered by scoreline`) with niche editorial descriptors. Six candidates scoped in the spec §15 with effort estimates and data requirements. **Cheap first-pass** (~3 hr incl. tests, data already in hand): `Defensive masterclass` (winner conceded < 0.5 xG), `Comeback win` (winner trailed at some point; uses running-score trajectory already in `fct_action_values`). **Medium lift** (~2 hr each + tests): `Against the run of play` (splits `Fortunate` by loser xG ≥ 2.0), `Late winner` prefix (go-ahead goal after minute 80), `Man-advantage winner` (opposing red card ≤ 60'). **Heavy lift**: `Won on set pieces` (≥60% of goals from set pieces) needs per-goal provenance verified in the shots/events mart. **Rationale for v1 deferral:** reader cognitive load — current 5 phrases are at the edge of what a tooltip can teach; adding compound phrases like `"Late-winner, fully merited"` expands the resolution-order test matrix rapidly. Purely additive — no breaking changes to add incrementally. **Revisit when:** user feedback surfaces verdict cases the current 5 phrases misrepresent, OR we want richer editorial voice on the tile before another redesign cycle. |
| HF1 | HF artifact: DFL Bundesliga events + tracking v2 datasets | Wicked | PR 1.8 bronze enrichment (2026-04-21) | **Prerequisite:** PR 2+ must surface the new bronze cols through staging first. **Scope:** publish `luxury-lakehouse/dfl-bundesliga-events-v2` — flat parquet of the 9,633 IDSSE events with all 241 source-complete cols (xG, shot_angle_to_goal, shot_distance_to_goal, amount_of_defenders, goalkeeper_behaviour, counter_attack, chance_evaluation, pass_direction, corner_placing, corner_rotation, penalty_direction, etc.). Community uptake potential is high: no pre-parsed DFL events dataset currently exists on HF — the Bassek et al. raw XML (Scientific Data 2025, CC-BY 4.0) requires custom parsing that we just productionised. Also publish `luxury-lakehouse/dfl-bundesliga-tracking-v2` — 26-col tracking parquet with speed / acceleration / direction / ball_status / ball_possession / ball_z. Attribution: Bassek et al. **Size estimate:** ~5 GB tracking parquet (partitioned by match_id, gzip) + ~5 MB events parquet. **Out of scope:** anything StatsBomb-derived (SB user agreement forbids raw redistribution; derived models are fine). |
| HF2 | HF artifact: xG v3 cross-provider (StatsBomb + IDSSE training) | Wicked | PR 1.8 bronze enrichment (2026-04-21) | **Prerequisite:** PR 2+ stages IDSSE events shot features. **Scope:** extend xG v2 training set with ~1,500 Bundesliga shots using IDSSE's directly-comparable features — `shot_angle_to_goal`, `shot_distance_to_goal`, `amount_of_defenders`, `goal_distance_goalkeeper`, `player_speed`, `pressure`, `chance_evaluation`, `player_speed`. Cross-provider training reduces variance and enables out-of-distribution calibration studies (holdout on Bundesliga matches; evaluate on Premier League matches or vice versa). Publish `xg-v3` model card (`docs/huggingface/model-cards/xg-v3.md`) documenting: (1) cross-provider training composition, (2) calibration plots on Bundesliga holdout, (3) feature-importance comparison with StatsBomb-only v2. **Rationale:** xG is our most-consumed model; adding a German-league signal broadens generalization and is a publishable calibration paper/blog. |
| HF3 | HF artifact: set-piece + throw-in feature tables (PA2/PA3 enablers) | Wicked | PR 1.8 bronze enrichment (2026-04-21) | **Prerequisite:** PR 2+ surfaces DFL nested attrs. **Scope:** pre-compute feature parquets from DFL nested structures: `fct_corner_features` (rotation / placing / target_area / post_marking / side + outcome), `fct_freekick_features` (execution_mode / direct_intention / team), `fct_penalty_features` (direction / execution / goalkeeper_behaviour / goalkeeper_movement / outcome), `fct_throwin_features` (side / decision_timestamp). Ship as HF datasets under `luxury-lakehouse/set-piece-features` and `luxury-lakehouse/throw-in-features`. **Enables** PA2 (Set Piece Effectiveness Page) and PA3 (Throw-In Analytics Page) with minimal page-side computation. **Stretch:** tiny specialized models can ride on the feature tables — `corner-to-shot-conversion` (logistic regression on corner features), `penalty-save-prediction` (logistic regression on goalkeeper_behaviour / goalkeeper_movement). Both small, publishable with model cards. |
| HF4 | Migrate `notebooks/publish_datasets.py` + `notebooks/publish_obso_data.py` to PEP 723 publishers using `ingestion.hf_publish` | Dunkin' | ADR-014 Consequences (2026-04-24, PR 4c) | **Current state:** the two Databricks notebooks still follow the pre-PR-4c `shutil.copy2(card_src, README.md)` + `api.upload_folder(...)` pattern rather than calling the shared helper. PR 4c updated their hardcoded card-path basenames so they publish the correctly-renamed files today (no active drift), but they remain outside the ADR-014 "every publisher uses `upload_hf_readme`" discipline. **Why they weren't migrated in PR 4c:** Databricks notebooks can't trivially import from the `luxury-lakehouse` wheel without adding a `%pip install https://...` cell at the top + `dbutils.library.restartPython()`, which changes the run contract; untested notebook refactor on a non-gated path was judged higher risk than the drift the migration removes. **Scope:** either (a) add the wheel install + replace `shutil.copy2` → `get_hf_card_path` + `upload_hf_readme` in each notebook cell, OR (b) migrate each cell into a PEP 723 `scripts/publish_*_hf.py` script and delete the notebooks. Either approach restores full conformance to ADR-014; (b) is slightly preferred because PEP 723 scripts get CI + the same wheel-import discipline as the existing publishers. **Covers:** `spadl-vaep-action-values` (second publisher, duplicate of `scripts/publish_spadl_vaep_hf.py`), `line-breaking-passes`, `pitch-control-tracking`, `football2vec-player-embeddings`, `xg-freeze-frame-data` (duplicate of `scripts/publish_freeze_frame_hf.py`), `obso-pausa-inputs`, plus the v1 model-card manual push at `publish_datasets.py:298+`. **Size estimate:** ~100-200 LOC per notebook migration; ~3 hours incl. manual end-to-end HF push verification. |
| D60 | Pass-Decision Quality — `fct_pass_epv` mart + Taipy section | Wicked | LinkedIn — John Comonitski EPV post (2026-04-21) + Hanan Abu Kwaider PhD framework (2026-04-21) | Add an EPV surface that makes pass-decision quality visible per-pass and per-player. Formula: `EPV = P(Success) · ΔxT + (1 − P(Success)) · Opp_ΔxT` composed from existing inputs — xT grids (`wf-xt-grids`, Singh 2018) for both ΔxT terms, pass-completion probability from event data (or VAEP's action probabilities). **Academic framing:** two-function pass evaluation (success probability + space value, independent of arrival), per Abu Kwaider (PhD thesis, 2026; live demo at soccer-analytics.streamlit.app). **Scope:** new `fct_pass_epv` mart at pass-event level with EPV score + rank vs alternative passes available in that frame (teammates visible to the passer per tracking data, top-K best-EPV options). Add a "Pass Decision Quality" section/page in Taipy reading the synced table. **Per-frame alternatives require tracking alignment** — join to ELASTIC-synced tracking tables (`wf-elastic-sync`) at ball-release timestamp to enumerate visible teammates. **Source coverage:** works on StatsBomb/Metrica/IDSSE/SkillCorner (tracking + events synced); Wyscout-event-only matches fall back to start-pos-only EPV (no teammate alternatives ranking) or are excluded. **Not in scope (defer):** pass-into-space variant (pitch sampling — heavy overlap with existing OBSO/Spearman 2018, belongs in ROADMAP as OBSO enhancement), learned completion probability model (use event-derived frequencies v1). **Compare with ScoutGPT:** rule-based; see D61 for the calibration follow-up. **Size estimate:** ~200-300 LOC (dbt mart + Python helpers + one Taipy section). |
| D61 | Decision Optimality Score (DOS) — EPV vs ScoutGPT calibration | Wicked | Follow-up to D60 + LinkedIn post thread (Malte Thesenvitz frame-by-frame comment) + Hanan Abu Kwaider PhD framework (2026-04-21) | Compare rule-based EPV (D60) with ScoutGPT's learned counterfactual ranking on the same pass-decision moments. **Naming:** adopt **Decision Optimality Score (DOS)** per Abu Kwaider's framework for both the mart column and the Taipy UI label — maximizes reader portability to the existing soccer-analytics literature. **Scope:** per-frame pass-decision table joining both rankings; agreement/disagreement histogram; per-player DOS = how close the passer's actual choice was to the theoretical best available at that frame (from EPV and/or ScoutGPT). **Requires:** D60 shipped (provides EPV ranks); ScoutGPT per-pass-decision inference path (new — currently only episode-level counterfactual exposed, need a per-frame inference wrapper). **Analytical value:** when EPV and ScoutGPT agree, high confidence; when they disagree, it's a "top players see something the value surface misses" moment worth highlighting. Also calibrates the rule-based EPV against learned ScoutGPT priors. **Out of scope:** model changes to ScoutGPT; this consumes existing inference only. **Size estimate:** ~150-250 LOC (per-frame ScoutGPT inference wrapper + join/aggregation mart + Taipy comparison view). |
| D62 | Pass-Failure Classification — extension to D60 EPV | Dunkin' | LinkedIn — Hanan Abu Kwaider PhD framework (2026-04-21) | Thin classifier on top of `fct_pass_epv` (D60) that tags every failed pass into three categories: **Passer Error** (high destination EPV × reasonable completion probability → execution fault), **Structure Error — Overambitious** (high destination EPV × low completion probability → risk misjudgment), **Structure Error — Trapped** (low best-available-EPV among alternatives → no good option existed). **Scope:** adds `failure_class` column to `fct_pass_epv` (derived from existing cols — no new inputs), per-player aggregation (Passer Error rate, Overambitious rate, Trapped rate), Taipy breakdown bar chart by failure type on the D60 page. **Per Abu Kwaider (2026)**, self-identified weakness: decay parameters distort "best available" in high-press moments — worth validating empirically with our own data before publishing the classification as-authoritative. **Requires:** D60 (EPV + alternatives per frame). **Size estimate:** ~50-100 LOC (column derivation + aggregation + UI section). |
| D63 | Decay-Weighted Pass Network — extension to `wf-pass-network` | Wicked | LinkedIn — Hanan Abu Kwaider PhD framework (2026-04-21) | Time-decaying edge weights on the pass-network graph so the visualization reflects "who is actively involved RIGHT NOW" versus the static whole-match average (current Pena & Touchette 2012 baseline). **Scope:** parameterize edge weight as `w = Σ e^(-λ·(t_now − t_pass))` over pass events, exposing `λ` (half-life) as a UI slider with sensible default (e.g., 5-10 min half-life). New mart column or live-compute variant in the Pass Network page. **Compare with static network** side-by-side to teach the temporal dynamics. **Known caveat per Abu Kwaider (2026)**: decay distorts evaluation during high-press / high-tempo segments where the "active" set changes faster than λ suggests — flag in UI and document the caveat. **Independent of D60/D61/D62** — can ship standalone. **Size estimate:** ~150-250 LOC (mart or page-side compute + slider + side-by-side panel). |
| D64 | ML-Intern calibration: autonomous paper→seed-program proposer | Dunkin' | LinkedIn 2026-04-22 (Hugging Face ml-intern release: `github.com/huggingface/ml-intern` + HF Space `smolagents/ml-intern` + `posttrainbench.com`) | Install `ml-intern` via `uv tool install -e .`, invoke with a prompt pointing at `src/evolve/targets/scoutgpt/seed_programs/` (format example: `cross_attention.py`) and Hong 2025 arXiv:2512.17266 as base architecture. The CLI is generic (`ml-intern "<prompt>"`), built on litellm (supports Anthropic/OpenAI API AND local via Ollama/LM Studio/vLLM); tool-router includes HF Jobs + sandbox + arXiv. **Phase 1 (calibration, frontier LLM ~$15):** does the agent rediscover ≥2 of our historical seed winners {cross_attention, fourier_cross_attention, swiglu, RoPE}? ≥2 → go to Phase 2. <2 → archive approach. **Phase 2 (local-LLM cost optimization, $0 ongoing):** rerun on DGX Spark hosting Llama 3.3 70B Q4 (fits the 128 GB unified memory). If local reproduces ≥80% of Phase 1's useful output, switch a weekly cron to local. **License caveat:** ml-intern repo has no LICENSE file — treat as read-only reference until that lands; do NOT redistribute or build proprietary extensions on top. **Out of scope:** PostTrainBench itself (LLM-only, benchmarks post-training of Qwen 3 / SmolLM3 / Gemma 3 — irrelevant to our per-player counterfactual ranking domain); auto-evaluation of agent-proposed seeds (that's a D65 follow-up if D64 clears Phase 1). Budget: $50 one-shot for Phase 1 + 1 hour human setup. |
| D66 | ExT-style xT v2 reproduction design spike | Dunkin' | T1 tracker (`docs/research/external-research-tracking.md`) — Salimi/Salmankhah LISS 2026-04-23 | Scope a KDE+KNN xT reproduction per the ExT methodology captured in T1. **Output:** design doc fixing (a) Optuna parameter space (KDE kernel + bandwidth, KNN K + distance + normalization, per-feature inclusion flags); (b) fitness function (held-out NLL inner loop, calibration outer selection from top-K trials); (c) phasing plan (Phase 0 Singh baseline → Phase 1 KDE-smoothed Singh → Phase 2 KNN-replaces-transition → Phase 3 contextual features incremental → Phase 4 feature exploration); (d) sizing — KNN query cost at our event volume + ANN backend choice (ball-tree vs HNSW vs already-in-place pgvector); (e) open questions for authors post-release. **Decision gate:** spike output promotes to a full reproduction TODO (Wicked or Monstah depending on Phase 3-4 sizing) OR returns to T1 tracker watch with concrete questions if blockers surface. **Adds Optuna as new dep** (MIT, well-maintained, zero current footprint). **Why a spike not a build:** ExT paper not public yet — methodology known at LinkedIn-comment-level granularity, sufficient for design but not implementation commitment. **Out of scope:** any neural xT successor work (longer-term peer bet captured in the paired ROADMAP "ExT-style Conditional xT" section). |

---

## Kimball Migration — Active Cycle

Cycle started 2026-04-20. Architectural basis: [ADR-011](docs/superpowers/adrs/ADR-011-unified-kimball-match-dimension.md). Target end-state: zero smart-keyed `match_id` columns across fact tables; every match reference is `match_key BIGINT FK → dim_matches`.

**Last updated 2026-04-22:** PR 1, PR 1.5, PR 1.6, PR 1.7, PR 1.8, PR 2 shipped. PR 2 also delivered the LB-IDSSE + LB-METRICA functional goal (IDSSE + Metrica passes landing in `fct_passes` with their line-breaking results rendering in Pass Map) AND a bronze→staging completeness sweep (every bronze column surfaced in staging; `INITIAL_BRONZE_STAGING_GAPS` closed across all 11 staging models; user directive "get bronze & staging to a point where we are done going back to rediscover things that we forgot") AND competition Kimball conformance (`dim_competitions.competition_key` BIGINT surrogate; IDSSE Bundesliga surfaces in Pass Map competition dropdown with line-breaking metrics). Current position: PR 3 next.

| PR | Scope | Status |
|----|-------|--------|
| 3 | Shots + xG migration — `int_unified_shots`, `fct_shots`, `fct_xg_predictions`, Taipy shot readers | Planned |
| 4 | Action values + VAEP migration — `int_running_score`, `fct_action_values`, SPADL/VAEP ingestion guard updates | Planned |
| 5b | **Player-side embedding + percentile mart migrations**: add `player_key` to `fct_player_embeddings` / `_season` / `_career` / `_season_360` / `_career_360` / `fct_player_percentiles` (six marts). Taipy consumer dual-read in `players.py` + `tracking.py` + `state/player_similarity.py`. HF dataset card 2026-07-22 dual-column documentation (cards only; payload drops in PR 8). Spec: `docs/superpowers/specs/2026-04-24-kimball-pr5-design.md` §2 "In scope — PR 5b". Retires the `dim_matches` bridge added to `fct_player_embeddings_season` + `_season_360` during PR 5a's CI triage. | Planned |
| 6 | Defensive + goalkeeper + pitch control migration + **IDSSE `is_progressive` tracking-source classifier** (separate compute from StatsBomb's event-derived flag; uses ball-position deltas + defending-line proximity from the tracking frame at pass-release; populates `fct_passes.is_progressive` for IDSSE rows — currently NULL for the tracking-only provider). | Planned |
| 7 | Tracking + formations + pausa + tail facts migration | Planned |
| 8 | Scripts + final cleanup + doc sweep (remove any transitional compat shims, close out migration in ARCHITECTURE.md) | Planned |

---

## Technical Debt

### Blocked or Deferred

| # | Item | Location | Description | Blocker |
|---|------|----------|-------------|---------|
| 1 | Synced tables Terraform workaround | `terraform/` | Must create synced tables via UI + import due to missing provider fields. `lifecycle { ignore_changes = all }`. No schedule/cron field on resource — SNAPSHOT refresh requires manual trigger or external job. Workaround: `scripts/refresh_synced_tables.py`. Root cause: the `/api/2.0/postgres/` surface (Autoscaling) has zero synced table endpoints — UI is the only method. The Provisioned API (`/api/2.0/database/synced_tables`) uses `database_instance_name` with no project/branch equivalent. GitHub issue filed: [terraform-provider-databricks#5456](https://github.com/databricks/terraform-provider-databricks/issues/5456). Related: [#5389](https://github.com/databricks/terraform-provider-databricks/issues/5389) (same gap for `databricks_database_database_catalog`). **Update 2026-03-06:** Connected with a Databricks Solution Architect at SSAC26 conference (LinkedIn). Bug report reference being forwarded for internal triage. | Blocked on Databricks API team adding synced table endpoints to `/api/2.0/postgres/`. Provider cannot be fixed until upstream API exists. |
| 6 | Line-breaking SkillCorner not yet wired | `line_breaking.py` | Path A (StatsBomb 360), Path B (Metrica tracking), and Path C (IDSSE tracking) are wired at ingestion (`line_breaking.py:127-129`). As of PR 1 of the Kimball migration (2026-04-20, ADR-011), Path B + C results are still NOT surfaced in `fct_passes` / `fct_match_summary` / Pass Map UI — that surfacing is scheduled for PR 2 of the same cycle. SkillCorner (10 matches) has tracking but no event data — cannot compute line-breaking without pass events. | SkillCorner: blocked on event data procurement or ball trajectory detection. |
| 7 | Single-frame 360 analysis | `line_breaking.py` | Path A uses opponent positions at pass moment only. Dual-frame would be more robust. | 360 freeze frames lack temporal resolution. Data limitation. |
| 9 | Fixed 3-cluster assumption | `analytics/line_breaking.py` | Ward clustering with `n_clusters=3` assumes 3 defensive lines. Breaks for 5-depth formations. | Research task — needs silhouette score analysis. |
| 10 | No set-piece exclusion | `analytics/line_breaking.py` | Corners, free kicks, throw-ins have non-standard formations. | Research task — needs `pass_type` filtering or set-piece-aware algorithm. |
| 11 | Heat Map pre-aggregation lossy | `heat_map.py` | Server-side `GROUP BY round(x/10)` bins into 10-yard cells before `bin_statistic`. Per-action precision lost. | Acceptable trade-off for density visualization. |
| 13 | PPDA StatsBomb-only | `fct_match_summary.sql` | PPDA uses StatsBomb defensive actions. NULL for Wyscout-only competitions (different event taxonomy). | Data limitation. Would require event type mapping or different pressing proxy. |
| 16 | Physical stats tracking-only | `fct_physical_stats.sql` | Only 20 matches (Metrica 3, IDSSE 7, SkillCorner 10) have physical data. ~3,000 event-only matches have none. | Data limitation — no tracking for StatsBomb/Wyscout. |
| 18 | DEFCON-lite anonymous defenders | `ingestion/defcon_lite.py` | StatsBomb 360 freeze frames are anonymous — `defender_player_id` is synthetic. `fct_defensive_values` cannot attribute credit to real defenders. Mitigated: `fct_defcon_pressure` pivots to attacker perspective (real `action_player_id`). | Full fix requires Tier 4 GNN with tracking data (500+ matches needed). |
| 25 | Lakebase CU right-sizing | Terraform | `autoscaling_max_cu = 4` may be overprovisioned for dev. Reduce to 2. | Blocked — Terraform provider cannot update `initial_endpoint_spec` after creation. Needs UI change. |
| 27 | Respo.Vision ingestion architecture | `src/ingestion/` | Respo.Vision 3D pose tracking (50+ keypoints × 22 players × 60fps = ~2.14B floats/match, ~17 GB raw) cannot use current ingestion patterns. Requirements: (1) streaming download (`requests.get(url, stream=True)` + chunked write to UC Volume), (2) Spark-native file reading (`spark.read.parquet()` or `spark.read.json()` — no pandas on driver), (3) incremental skip guard on `match_id`, (4) `applyInPandas` for all per-match analytics (driver must never see raw tracking data), (5) schema decision: narrow `(match_id, frame_id, player_id, keypoint_id, x, y, z)` ~2B rows/match vs semi-narrow `(match_id, frame_id, player_id, keypoints_json)` ~14M rows/match. Design before any data arrives. | Blocked on own-footage recording + Respo.Vision processing. |
| 28 | Databricks budget automation | `terraform/` | AWS budget is automated via `aws_budgets_budget` ($100/month, 80%/100% alerts). Databricks spending alerts are manual (UI-only, $250/month set 2026-03-12). Investigate whether Databricks Budgets API or `databricks_budget` Terraform resource can automate this for consistency. | Next infrastructure cycle. |
| 29 | SEC4 INF-01 full closure — remove CI SP workspace-admins-group membership | `terraform/modules/service_principals/main.tf:80-87` | The SEC4 cycle (2026-04-17) reduced transitive-admin paths for Lakebase + pipelines but left `databricks_group_member.terraform_ci_admin` in place. Three attempts (`api = "account"`, `provider = databricks.account`, targeted apply) all failed on TF-planner cascade: any pending update on an SP resource marks its `id` as "known after apply", which propagates through `data.databricks_service_principal.*_account` to `databricks_group_member.dbt_owners_*.member_id` (force-new), triggering destructive replacement of 2 group members. Full evidence in [ADR-007](docs/superpowers/adrs/ADR-007-workspace-admin-floor.md) §Alternatives. | Blocked on upstream Databricks provider enhancement that decouples SP update plans from downstream `databricks_group_member.member_id` reference stability, OR restructuring the data sources to hardcode application_ids (loses reference integrity — judgment call), OR moving SP creation out of Terraform entirely (multi-cycle refactor). |
| 31 | StatsBomb team IDs + manager IDs via `fmt='json'` re-fetch | `src/ingestion/statsbomb.py` (ingestion) / `_statsbomb__sources.yml` (staging) | statsbombpy's default `fmt='dataframe'` flattens team + manager objects to display strings, losing `home_team_id`, `away_team_id`, `home_team.country`, `home_managers[].id`, etc. Re-fetching via `fmt='json'` and extracting nested IDs would complete bronze-completeness for StatsBomb matches. Option 4 from the 2026-04-20 cross-provider gap audit; backlogged out of PR 1.5 scope per user directive. **Re-ingest-once-eliminated** per bronze-completeness principle — needs a one-shot ingestion re-run after code lands. | Backlog — not urgent (IDs are not required for current analyses). |
| 32 | Metrica pseudo-competition in `dim_competitions` | `dbt_project/models/marts/dim_competitions.sql` | Metrica sample data has no competition field — all 2,884 Metrica passes in `fct_passes` carry NULL `competition_key` and are therefore invisible in Pass Map's competition cascade. Minimal fix: add a Metrica pseudo-row to `dim_competitions` (e.g. `competition_id = NULL`, `native_competition_id = 'metrica-sample'`, `competition_name = 'Metrica Sample Dataset'`, `country = NULL`), then populate `stg_metrica__passes.competition_id` with the same sentinel so `int_unified_passes` carries a non-NULL `competition_key`. Once wired, Metrica's 3 sample matches become selectable from the Pass Map filter cascade. Discovered during the PR 2 / competition Kimball conformance E2E on 2026-04-22. | Follow-up cycle. Not blocking — Metrica's functional pass data is in `fct_passes`; only UI surface is missing. |
| 33 | IDSSE / tracking-source `is_progressive` detection | `dbt_project/models/staging/idsse/stg_idsse__passes.sql` | `is_progressive` is currently populated only for StatsBomb and Wyscout (both use source-native flags). IDSSE tracking passes uniformly return 0 in Pass Map's Progressive metric because `stg_idsse__passes.is_progressive` is hardcoded false. A tracking-source progressive classifier (e.g., end-to-goal distance relative to pass-length threshold, or the standard ≥30% reduction in distance to opponent goal for upfield passes) would close this gap for DFL matches and for future SkillCorner / Metrica wiring. Discovered in the PR 2 IDSSE E2E on 2026-04-22 (629 passes, 0 progressive). | Follow-up cycle. Not blocking — line-breaking (the harder metric) already works on IDSSE tracking. |
| 34 | `stg_metrica__tracking` Mode-4 parity test (unpivot cardinality) | `src/tests/test_staging_rowcount_vs_bronze.py` | G3 of the 2026-04-22 drop-safety sweep intentionally excluded `stg_metrica__tracking` from the rowcount-parity test because its filter (`where raw_x is not null and raw_y is not null`) runs on the `all_players` CTE — a `lateral view explode` unpivot of `home_players` / `away_players` JSON maps. Post-unpivot row count is ~22× the bronze row count, so the direct invariant `count(staging) + count(bronze WHERE filter-null) == count(bronze)` does not apply. A staging-to-staging parity test that materialises the unpivot baseline and counts drops against that would close the gap. Alternative: a CTE-level test that re-runs the unpivot in the test query and counts the filter-dropped rows. | Follow-up cycle. Not blocking — the other 4 filter sites are covered; Metrica tracking has only 3 sample matches and the filter is unlikely to silently leak rows at that scale. |
| 30 | Taipy shared-state race on team-change-after-comp-change | `hf_taipy_app/src/state/shared.py` | Observed during D58 staging verification (2026-04-18): when the user switches competition then immediately picks a team, the first team-click's `on_team_change` is occasionally coalesced with the comp-change update and dropped — the user has to pick a different team, then re-select the intended one. Only visible on pages that require a team (Conversion Funnel); comp-only pages (Heat Map) render immediately after team reset to "All". Same class as the "destabilizing filter propagation" issue that descoped PA1's shared game-state dropdown. `on_competition_change` resets `selected_team = _ALL_LABEL` (line 292) then calls `_refresh_current_page` (line 312), but the subsequent team-click's WebSocket message appears to race with the in-flight shared-state reset. Pre-existing (not a D58 regression). **Update 2026-04-18 (Heat Map UI cycle, branch `ui/heat-map-context-and-filters`):** (1) The Heat Map UI cycle's initial design had `on_competition_change` and `on_team_change` each calling `search_players("")` after `fetch_players()` — an extra ~150-300ms SQL round-trip that measurably widened the race window. Mitigated by slicing `fetch_players()[:50]` for the single-select LOV instead. Look for the "TODO.md #30" comments in `state/shared.py` before reintroducing that kind of duplicate query. (2) New callbacks added in the same cycle — `on_player_search_change` et al. — write `state.<prefix>_player_lov` from typed-search events; if a user types in the player search input _during_ an in-flight comp-change refresh, this is a second class of state-write race on the same cascade. Not observed in E2E testing (local, 2026-04-18) because search fires only after 300ms debounce + user intent, but worth including in #30's repro matrix. (3) Empirically-related Taipy reactivity quirk: `state.X_lov = []` (empty list) is treated as a no-op by the state diff, leaving the previous LOV visible. Worked around with a `NO_MATCHES_SENTINEL = "(no matches)"` placeholder in `filters.py`. That's the _opposite_ problem from #30 (state diff too strict vs. too loose) but points at the same reactivity layer — likely the same root cause fix would address both. | Investigation needed — possibly a Taipy 4.1 WebSocket dedupe behavior. Workaround for users: pick any other team first, then the intended one. Options: (a) queue team-change events during comp-change processing, (b) debounce team-dropdown clicks, (c) force-fire team-change via `state.selected_team = None; state.selected_team = picked_value` (same pattern already used for the empty-LOV quirk — see `NO_MATCHES_SENTINEL`), (d) upstream Taipy fix. |
| 33 | ScoutGPT cross_attention conditioning — padding leak through K/V over player_emb | `src/analytics/scoutgpt_decoder.py` (`_embed`, `cross_attention` branch, ~line 222-224) | Surfaced by `test_scoutgpt_rope.py::test_rope_padding_mask_preserved` during the 2026-04-22 cross_attention default-flip cycle. The `cross_attention` (and by inheritance `fourier_cross_attention`) `_embed` branch calls `self.player_cross_attn(query=action_emb, key=player_emb, value=player_emb)` without a `key_padding_mask`, so padded positions' player embeddings contribute to the cross-attention output at every valid position — scrambling padded `player_ids` silently shifts logits at valid positions. Additive conditioning does not have this issue (it just adds `player_emb[i]` to `action_emb[i]` per-position, no cross-position mixing). **Workaround applied 2026-04-22**: test_scoutgpt_rope's `_small_config()` explicitly pins `conditioning_type="additive"` so RoPE's mask-preservation invariant can be tested unconfounded; see the function's docstring. **Proper fix (deferred):** extend `_embed(...)` signature to accept `attention_mask: torch.Tensor \| None = None`, update `forward(...)` / `predict(...)` to pass it through, convert to `key_padding_mask=~attention_mask` on the three cross-attn calls (`player_cross_attn` in `cross_attention` and `fourier_cross_attn` in `fourier_cross_attention`). Add a test matrix that verifies the padding invariant holds for every non-additive conditioning value at every position_embedding value. **Impact** on shipped checkpoints: real-world batches have `player_ids` padded to the same fill value at padded positions (typically player 0), so the leak degrades to "every valid position gets one extra attention term toward player 0's embedding" rather than arbitrary scrambling — measurable but small bias, not outright corruption. Still worth fixing before any customer-facing inference path uses variable-length batches. | Scope needs ~30-50 LOC + test-matrix additions. Not urgent for research cycles (fixed-length batches dominate), important for any production inference API. |

### Deferred EIP / Optimization Items

Items from the Pipeline Optimization & Scaling (EIP) roadmap section that were evaluated and deferred. Core EIP patterns (Splitter, Aggregator, Router, Pipes & Filters) are already implemented and codified in CLAUDE.md.

| # | Item | Description | When to revisit |
|---|------|-------------|-----------------|
| D22 | NannyML CBPE for Model Monitoring | Evaluated 2026-03-25. All current models have immediate ground truth — CBPE's value (performance estimation without ground truth) does not apply. Existing scipy-based drift detection (PSI, Wasserstein, KS, CUSUM) covers all validation scenarios. | When a real-time inference use case appears where ground truth is delayed. |
| E2 | Change Data Feed (CDF) | Delta `table_changes()` for incremental downstream consumption. No downstream consumer currently needs change tracking. | When a streaming consumer (e.g., real-time dashboard, ML feature store) is added. |
| E3 | Dead Letter Channel | Failed record quarantine to `bronze.dead_letters` table. Current retry logic handles transient errors; no persistent failure pattern observed. | When ingestion sources become unreliable or data volume exceeds manual inspection. |
| E4 | `dbt clone` for staging | Zero-copy table references for pre-production validation. Requires Lakebase branching. | When staging environment (ROADMAP.md) is implemented. |
| E6 | Delta retention policy | Explicit `delta.deletedFileRetentionDuration` (30d gold, 7d bronze) ahead of DBR 18.0. | Before DBR 18.0 upgrade where `RETAIN X HOURS` in manual VACUUM is ignored. |
| E7 | `fct_player_embeddings_career/season` incremental | Both models use `materialized='table'` (full rebuild). Acceptable: ~8,950 rows each, simple `AVG()` over ~87K source rows (~3 seconds), guarded by `enabled=var('embeddings_enabled', false)` so they only run on explicit request. Incremental alternative (track which players have new matches, recompute per-player means, merge) adds state-tracking complexity that exceeds the full-rebuild cost at this scale. Break-even: source table >500K rows or rebuild >30 seconds. Evaluated 2026-03-31 (OPT-AUDIT). | When `fct_player_embeddings` exceeds 500K rows or rebuild time exceeds 30 seconds. |
| U5 | Cross-page contextual links in Taipy | No contextual "see also" links between related pages (e.g., Player Impact → Player Comparison, Defensive Impact → Player Impact). The template architecture supports `ContentBlock("text", ...)` for inline links, but which pages to connect and where to place links requires UX design decisions. CHI-AUDIT C-10 identified this as a navigation design opportunity. Evaluated 2026-03-31. | When the next Taipy UI cycle adds new pages or the shape graph visualization pages are built (ROADMAP.md). |

---

## Research & Future Work

See [ROADMAP.md](ROADMAP.md) for research directions, long-horizon features, and unscheduled ideas including:

- **Observability Layer (OpenTelemetry)** — instrument once, observe anywhere; ~$1-2/month personal tier
- **Deep Learning Infrastructure** — GNN training, continual learning, pre-trained model integration
- **Provider Abstraction** — configurable multi-tier ingestion; free/open tiers default, commercial activates via credentials
- **Visual Exploratory Behavior** — partially unblocked: 6 Veo3 recordings + local RTMO pose estimation feasible (BSD 3-Clause)
- **Staging Environment** — Lakebase branching for pre-production validation
- **Graph-Based Tactical Patterns** — GNN research direction (Raabe et al. 2022)
- **Decision Optimization** — RL-based pass optimization beyond VAEP (Rahimian et al.)
- **Hugging Face Hub** — Tier 5 (streaming dataset publishing via XET + Polars) blocked on upstream

## Infrastructure Notes

Infrastructure IDs are environment-specific. Use `terraform output` for current values.

| Resource | Env Var / Source |
|----------|----------------|
| AWS region | `us-east-1` |
| AWS profile | `AWS_PROFILE=devops-agent` |
| Databricks workspace URL | `DATABRICKS_HOST` env var |
| Unity Catalog | `soccer_analytics` |
| SQL Warehouse ID | `terraform output sql_warehouse_id` |
| Lakebase project ID | `terraform output lakebase_project_id` |
| Lakebase endpoint | `terraform output lakebase_endpoint_name` |
| Lakebase DNS (RW) | `terraform output lakebase_read_write_dns` |
| Ingestion job ID | `DATABRICKS_JOB_ID` env var / `terraform output ingestion_job_id` |
| App URL (Taipy) | [huggingface.co/spaces/luxury-lakehouse/soccer-analytics-app](https://huggingface.co/spaces/luxury-lakehouse/soccer-analytics-app) |
| GitHub Actions IAM Role | `terraform output github_actions_role_arn` |
| State KMS Key | `terraform output state_kms_key_arn` |
| Terraform CI SP | `terraform output terraform_ci_sp_application_id` |
| GitHub repo | `karsten-s-nielsen/luxury-lakehouse` (private) |
| Monthly budget | Under $100 |
| Terraform state bucket | `karstenskyt-terraform-state` (S3 native locking) |
| Start Claude Code with | `AWS_PROFILE=devops-agent claude` |
