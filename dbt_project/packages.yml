packages:
  # General-purpose dbt utility macros (surrogate_key, star, pivot, etc.)
  - package: dbt-labs/dbt_utils
    version: [">=1.1.0", "<2.0.0"]

  # Data quality testing framework (expect_column_values_to_be_between, etc.)
  - package: calogica/dbt_expectations
    version: [">=0.10.0", "<1.0.0"]
