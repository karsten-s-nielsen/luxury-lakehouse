# silly-kicks 4.118.0 Adoption — P0 Foundation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Move the lakehouse onto silly-kicks 4.118.0 and land every change that must precede any new-metric materialization or corpus recompute — the version bump + all lockstep pins/sentinels, the AC-drain elastic-v2 breaking-signature adoption, the B1 elastic delete-and-depend, and the synced-table CI-failure fix.

**Architecture:** silly-kicks is the SPADL/VAEP/xT/tracking engine. This plan changes only version pins and the code that breaks or must adopt across 4.90.1→4.118.0; it materializes nothing new (P1) and runs no recompute (P2). It is CODE-only + CI-green; every value-changing recompute is deferred to P2 operator runs behind explicit approval.

**Tech Stack:** Python 3.10, uv, Ruff, Pyright, pytest, PySpark (Databricks serverless), dbt (databricks), Terraform, silly-kicks 4.118.0.

**Spec:** `docs/superpowers/specs/2026-09-17-silly-kicks-4118-adoption-design.md` (§2.3, §3, §5.4, §6 step 1, §8). Executors read both.

## Global Constraints

- **Python 3.10 locked** (`>=3.10,<3.11`); line length **120**.
- **TARGET PIN = silly-kicks `4.120.0`** (tag `v4.120.0`, sk main). Bumped 4.119.0→4.120.0 (owner: "include both" the 4.119.1 packing fix + 4.120.0 TF-54b Path B). 4.120.0 is additive over 4.119.0/4.118.0 and CONTAINS: SK-EXPORT `metric_contracts` (4.119.0) + the `secured_reception` robust-sort packing fix (4.119.1 — **unblocks the Task 2 full-golden regen**) + TF-54b Path B (the `territory` counterfactual method — a NEW metric surface, folded into P1 metric #5). Every pin/sentinel target in P0/P1/P2 = `4.120.0` / `(4, 120, 0)`. NOTE: the per-feature sk release numbers in the spec's §2 classification table (match_outcome 4.118.0, elastic 4.114, etc.) are unchanged historical facts — do NOT rewrite those; only the *target pin* moves. Auth prerequisite lifted (owner local).
- **silly-kicks pin = `silly-kicks[das,ghost-gk,parse-dfl]==4.120.0`** (Terraform env, ADR-046 exact pin) and floor `>=4.120.0,<5` (pyproject). Never hand-edit TF env pins or the pre-commit ruff rev — regenerate via `uv run python scripts/sync_tf_env_pins.py`.
- **All 7 CI gates + pip-audit must pass before any commit:** `ruff check src/ scripts/`, `ruff format --check src/ scripts/`, `lint-imports`, `bump_wheel.py --check`, `pip_audit_ignores.py --check`, `pyright src/ hf_taipy_app/src/ scripts/_tf_env_pins.py scripts/sync_tf_env_pins.py`, full `pytest src/tests/`, `pip-audit`.
- **Approval gates:** no `git commit`, `git push`, `gh pr create`, operator run, or `gh pr merge` without a separate explicit human "yes". Approval of this plan is NOT commit authority.
- **One feature branch**, minimal fully-tested commits, squash-merged at the end. Branch: `feat/sk4118-adoption`.
- **ADR references:** lakehouse-repo by default (ADR-013/043/046/072/082/014); silly-kicks-series ADRs are marked `sk:ADR-NNN`.
- **Recompute is over the EXISTING corpus only** (P2); no new match ingestion.
- **Watch every new/edited guard fail first** before it passes (a never-red gate is unproven).

---

## File Structure

- `pyproject.toml` — silly-kicks floor; `scripts/_tf_env_pins.py` inputs unchanged.
- `terraform/modules/workflows/main.tf` — `silly-kicks[...]==4.120.0` env pin (via sync tool).
- `.pre-commit-config.yaml` — ruff rev (via sync tool, if uv.lock ruff moved).
- `uv.lock` — `uv lock --upgrade-package silly-kicks` (pulls ruthless-efficiency 0.6.0 + transitives).
- `src/ingestion/{bravery_writer,defensive_credit_writer,exec_visibility,gkdv_writer,off_ball_runs_writer,xt_gk_v2_writer}.py` — `_REQUIRED_SK_MIN → (4, 120, 0)`.
- `scripts/train_{football2vec,football2vec_360,football2vec_v2,scoutgpt_hf,vaep_model_hf,xg_v3_hf,xt_gk_v2_hf}.py` — `_REQUIRED_SK_MIN → (4, 120, 0)`.
- `scripts/submit_ac1_oneshot.py` — PEP 723 pin `==4.120.0`.
- `src/tests/test_sk3_mig_b_orchestrator_invariants.py`, `src/tests/test_train_xt_gk_v2.py` — hardcoded `(4, 120, 0)` tuples + comments.
- `src/analytics/action_context/enrich.py`, `src/analytics/action_context/schema.py` — AC elastic-v2 signature + 3 new `elastic_receive_*` columns.
- `src/analytics/action_context/` goldens (mini-golden + drain golden) — regenerate.
- `src/ingestion/elastic_sync.py` — migrate onto sk `align_events_to_frames` (B1).
- `src/analytics/elastic_sync.py` + its tests — **delete** (B1).
- PAUSA/OBSO consumers (`src/ingestion/pausa.py`, `scripts/publish_obso_pausa_inputs_hf.py`), AC oracle fixture (`scripts/extract_action_context_fixture.py`, `src/tests/action_context/oracle_map.py`), dbt idsse source (`dbt_project/models/staging/**/_idsse__sources.yml`), guard-module registry — rewire for B1.
- CI-fix: `scripts/maintain_synced_tables.py` / `scripts/migrate_synced_tables.py` / the synced-table health check + any missing OIDC `vars` in `.github/workflows/lakebase-grants.yml` / `data-quality-ci.yml` (diagnosis-dependent; §8).

---

### Task 1: Version bump + lockstep pins/sentinels

**Files:**
- Modify: `pyproject.toml` (silly-kicks floor), `uv.lock`, `terraform/modules/workflows/main.tf`, `.pre-commit-config.yaml`, the 6 ingestion + 7 trainer `_REQUIRED_SK_MIN`, `scripts/submit_ac1_oneshot.py`.
- Test: `src/tests/test_sk3_mig_b_orchestrator_invariants.py`, `src/tests/test_train_xt_gk_v2.py`, `src/tests/test_executor_env_guard.py`, `src/tests/test_terraform_env_dep_parity.py`, `src/tests/test_ci_dbt_pin_parity.py`.

**Interfaces:**
- Produces: silly-kicks 4.120.0 resolvable + pinned everywhere; every `_REQUIRED_SK_MIN == (4, 120, 0)`; TF env pin `==4.120.0`.

- [ ] **Step 1: Update the sentinel expectations first (red).** In `test_sk3_mig_b_orchestrator_invariants.py` change `expected = (4, 90, 1)` → `(4, 120, 0)` and the `§2.10.5` comment; in `test_train_xt_gk_v2.py` change `assert trainer._REQUIRED_SK_MIN == (4, 90, 1)` → `(4, 120, 0)`.

- [ ] **Step 2: Run the sentinels to verify they now FAIL against the un-bumped constants.**

Run: `uv run pytest src/tests/test_sk3_mig_b_orchestrator_invariants.py src/tests/test_train_xt_gk_v2.py -q`
Expected: FAIL — trainers still declare `(4, 90, 1)`.

- [ ] **Step 3: Bump every `_REQUIRED_SK_MIN` to `(4, 120, 0)`** across the 6 `src/ingestion/*` + 7 `scripts/train_*` modules (see File Structure). Bump `scripts/submit_ac1_oneshot.py` PEP 723 pin to `silly-kicks[das,ghost-gk,parse-dfl]==4.120.0`.

- [ ] **Step 4: Bump the pyproject floor.** `pyproject.toml`: `"silly-kicks[das,ghost-gk,parse-dfl]>=4.120.0,<5"`.

- [ ] **Step 5: Re-lock.**

Run: `uv lock --upgrade-package silly-kicks`
Expected: silly-kicks → 4.120.0; ruthless-efficiency → 0.6.0 (+ transitives). Capture the resolved set.

- [ ] **Step 6: Sync the TF env pin + pre-commit ruff rev.**

Run: `uv run python scripts/sync_tf_env_pins.py`
Expected: `terraform/modules/workflows/main.tf` silly-kicks `==4.120.0`; any ruff-rev / CI-dbt-pin drift corrected. Never hand-edit.

- [ ] **Step 7: Run the parity + guard tests (green).**

Run: `uv run pytest src/tests/test_sk3_mig_b_orchestrator_invariants.py src/tests/test_train_xt_gk_v2.py src/tests/test_executor_env_guard.py src/tests/test_terraform_env_dep_parity.py src/tests/test_ci_dbt_pin_parity.py -q`
Expected: PASS.

- [ ] **Step 8: Sync the venv to the new lock** (needed for the rest of P0's local runs).

Run: `uv sync --extra analytics --extra embeddings --extra mlflow --extra jax`
Expected: silly-kicks 4.120.0 installed; `.venv/Scripts/python.exe -c "import silly_kicks; print(silly_kicks.__version__)"` → `4.120.0`.

*(No commit yet — Task 1 is not independently shippable; the bump breaks the elastic signature until Task 2/3. Commit at Task 5's gate.)*

---

### Task 2: AC-drain elastic-v2 signature + schema adoption (MANDATORY)

**Files:**
- Modify: `src/analytics/action_context/enrich.py` (the `add_elastic_sync` call at ~:456 + import block ~:255), `src/analytics/action_context/schema.py` (add the 3 `elastic_receive_*` columns to `_AC_COLUMNS` + the DDL string).
- Regenerate: AC mini-golden fixture + drain golden.
- Test: `src/tests/action_context/test_*` (schema, golden), `src/tests/action_context/test_sk_frame_adapters.py`.

**Interfaces:**
- Consumes: silly-kicks 4.120.0 `silly_kicks.tracking.add_elastic_sync` (TF-57 v2 signature; greedy weights removed).
- Produces: AC output carries `elastic_receive_frame_id BIGINT`, `elastic_receive_confidence DOUBLE`, `elastic_receive_error_seconds DOUBLE` (names verbatim from sk:ADR-093 / CHANGELOG 4.114) alongside the existing `elastic_frame_id/_confidence/_error_seconds` (values move).

- [ ] **Step 1: Read the new sk signature.** In `.venv`, introspect: `python -c "import inspect,silly_kicks.tracking as t; print(inspect.signature(t.add_elastic_sync))"`. Confirm the removed greedy-weight kwargs and the new reception-column outputs.

- [ ] **Step 2: Write the failing schema test.** Extend the AC schema test to assert the three `elastic_receive_*` columns are present in `_AC_COLUMNS` and the DDL.

```python
def test_ac_schema_has_elastic_receive_columns():
    from analytics.action_context import schema

    for col in ("elastic_receive_frame_id", "elastic_receive_confidence", "elastic_receive_error_seconds"):
        assert col in schema._AC_COLUMNS
        assert col in schema.ACTION_CONTEXT_DDL
```

- [ ] **Step 3: Run it (fail).** Run: `uv run pytest src/tests/action_context/ -k elastic_receive -q` → FAIL (columns absent).

- [ ] **Step 4: Update the call + schema.** In `enrich.py` adopt the new `add_elastic_sync(...)` signature (drop removed kwargs; keep it on the AC `tracking_df`). In `schema.py` append the 3 columns to `_AC_COLUMNS` (typed BIGINT/DOUBLE/DOUBLE) and to the DDL string, in the order sk emits them.

- [ ] **Step 5: Run the schema test (pass).** Run: `uv run pytest src/tests/action_context/ -k elastic_receive -q` → PASS.

- [ ] **Step 6: Regenerate BOTH AC goldens** (mini-golden CI gate + drain golden) — the AC columns changed (elastic values move + 3 new). Follow the repo's golden-regen procedure (`scripts/` AC-fixture regeneration; both the 3-action IDSSE mini-golden and the drain golden). Verify the regenerated goldens contain finite `elastic_receive_*` where linkage exists and honest-NaN on freeze-frames (SB360 `POSITIONAL_ONLY`).

- [ ] **Step 7: Run the AC test suite (pass).** Run: `uv run pytest src/tests/action_context/ -q` → PASS.

*(No standalone commit — folded into the Task 5 foundation commit.)*

---

### Task 3: B1 elastic delete-and-depend (Option B-ac — action grain, source-from-AC; spec §5.4)

**Decision (owner 2026-09-20, Option B-ac):** RETIRE the standalone `wf-elastic-sync` producer + the local greedy engine; the action-grain elastic columns (`elastic_frame_id/_confidence/_error_seconds` + `elastic_receive_*`) are the AC drain's output in `fct_action_context` / `stg_action_context__values` (IDSSE rows). Re-point every consumer of `bronze.elastic_sync_results` to that AC source. One engine, one grain, **one producer** (the AC drain). Values are identical because they ARE the AC drain's — no second aligner run, no `MatchMeta`/frame-conversion duplication.

**Files:**
- Delete: `src/analytics/elastic_sync.py` + its unit tests; `src/ingestion/elastic_sync.py` (the whole `wf-elastic-sync` producer) + its tests.
- Remove the `wf-elastic-sync` mega-job task everywhere it is registered: the TF task block in `terraform/modules/workflows/main.tf`, the workflow card (`config/**/*elastic*` card file), the `dbt_project/seeds/task_workflow_mapping.csv` row, the card→module map, and any `_GUARD_MODULES` / skip-guard registration in `src/ingestion/guards.py`.
- Create: `scripts/migrations/2026-09-20-drop-elastic-sync-results.sql` — idempotent `DROP TABLE IF EXISTS {catalog}.{bronze}.elastic_sync_results` (destructive, operator-applied with the merge per the bronze-migration convention; no re-create — the table is retired).
- Modify (re-point to the AC elastic source, action grain): `src/ingestion/pausa.py` (read the IDSSE elastic columns from the AC output keyed on `action_id`, not `bronze.elastic_sync_results` on `event_id`); `scripts/publish_obso_pausa_inputs_hf.py` (INNER JOIN `idsse_events ON event_id` → the AC source on `action_id`; payload re-sourced from AC/SPADL columns; select the 7 elastic cols) + the OBSO/PAUSA **HF card** (`get_hf_card_path(...)` — grain event→action) + any artifact-list references; `scripts/extract_action_context_fixture.py` (derive the elastic oracle from the AC output, drop the standalone `elastic_sync_results` extraction); `src/tests/action_context/oracle_map.py` (legacy-oracle note); the dbt idsse source `dbt_project/models/staging/idsse/_idsse__sources.yml` (remove the `elastic_sync_results` source) + `dbt_project/models/staging/idsse/stg_idsse__elastic_sync.sql` (retire, OR repoint as a thin VIEW selecting the IDSSE elastic cols from `stg_action_context__values` to preserve the name for any dbt-ref consumer — decide in Step 4 by grepping refs to `stg_idsse__elastic_sync`).
- Test: consumer tests (pausa / obso re-pointed to the AC source); a structural gate asserting (a) no `wf-elastic-sync` task in the seed/TF, (b) zero readers of `bronze.elastic_sync_results`, (c) zero importers of `analytics.elastic_sync` / `ingestion.elastic_sync`.

**Interfaces:**
- Consumes: the AC drain's elastic columns on `stg_action_context__values` / `fct_action_context` (`data_source='idsse'`): `action_id, native_match_id, elastic_frame_id, elastic_confidence, elastic_error_seconds, elastic_receive_frame_id, elastic_receive_confidence, elastic_receive_error_seconds`.
- Produces: PAUSA + OBSO/PAUSA HF publish sourced from the AC elastic columns; `wf-elastic-sync` + `bronze.elastic_sync_results` + `analytics/elastic_sync.py` gone. One engine, one grain, one producer.

- [ ] **Step 1: Confirm the AC source exposes the 7 elastic cols for IDSSE.** `stg_action_context__values` casts `elastic_frame_id/_confidence/_error_seconds` (:160-163) + the new `elastic_receive_*` (added in Task 2); confirm they carry `action_id` + `native_match_id` + `data_source` for the IDSSE filter/join. (AC full golden already shows `elastic_receive_*` populated 54/97 — evidence the AC path emits them.)

- [ ] **Step 2: Write the failing tests.** (a) A consumer test: `publish_obso_pausa_inputs_hf` / `pausa` read elastic from the AC source keyed on `action_id` (assert the produced column set + a known IDSSE alignment). (b) A structural gate: grep-based test asserting no `wf-elastic-sync` in `task_workflow_mapping.csv` / `main.tf`, no `elastic_sync_results` reader in `src/`+`scripts/`+`dbt_project/models`, no `analytics.elastic_sync`/`ingestion.elastic_sync` importer.

- [ ] **Step 3: Run them (fail).** Consumers still read `bronze.elastic_sync_results` on `event_id`; the standalone module + task still exist → FAIL.

- [ ] **Step 4: Re-point consumers to the AC source (same commit).** `pausa.py`, `publish_obso_pausa_inputs_hf.py` (+ HF card + artifact-list refs), `extract_action_context_fixture.py`, `oracle_map.py`, the dbt idsse source + `stg_idsse__elastic_sync.sql` (grep `ref('stg_idsse__elastic_sync')` first: 0 refs → delete; ≥1 → repoint as a view over `stg_action_context__values`).

- [ ] **Step 5: Delete the producer + engine + retire the task.** Remove `src/ingestion/elastic_sync.py`, `src/analytics/elastic_sync.py` + their tests; strip the `wf-elastic-sync` TF task + card + seed row + card-map + guard registration. Grep-verify zero importers: `grep -rn "analytics.elastic_sync\|ingestion.elastic_sync" src/ scripts/ hf_taipy_app/ | grep -v .venv` → empty.

- [ ] **Step 6: Write the DROP migration.** `scripts/migrations/2026-09-20-drop-elastic-sync-results.sql` — `DROP TABLE IF EXISTS`.

- [ ] **Step 7: Run consumer + structural tests (pass), plus the mega-job parity tests** (`test_sk3_mig_b_orchestrator_invariants.py` seed↔TF↔orchestrator; any card-inventory completeness test) so removing the task stays consistent. Run: `uv run pytest src/tests/ -k "elastic or pausa or obso or workflow_card or task_mapping" -q` → PASS.

- [ ] **Step 8: Lint-imports** (the deletes remove `ingestion→analytics.elastic_sync`; confirm layering intact). Run: `uv run lint-imports` → contracts kept, 0 broken.

*(The `DROP TABLE` migration is operator-applied with the merge. The elastic values re-materialize with the AC drain in P2 — no separate producer run. This task is code + migration file only.)*

---

### Task 4: Synced-table CI-failure fix (Data Quality CI + Lakebase Maintenance)

**Files:**
- Investigate (P2-live): the ~25 synced tables in `SYNCED_TABLE_ONLINE` (pending update) vs expected `SYNCED_TABLE_ONLINE_NO_PENDING_UPDATE`.
- Modify (if diagnosis warrants): `.github/workflows/lakebase-grants.yml` / `.github/workflows/data-quality-ci.yml` (the early OIDC `vars.HF_APP_SP_APPLICATION_ID` / `DATABRICKS_CLIENT_ID` `::error::`), and/or the synced-table health-check tolerance module, and/or a runbook note. The settle itself (`scripts/delete_synced_table.py` + `scripts/migrate_synced_tables.py` / `rederive_synced_marts`) is a P2 operator run.
- Test: the synced-table health-check unit (if a code change is made).

**Interfaces:**
- Produces: both scheduled jobs green after P2's synced re-derive; any code/config fix landed here.

- [ ] **Step 1: Classify (needs LIVE Databricks — gated to execution).** Determine whether the pending-update state is (a) a transient/queued TRIGGERED-sync backlog that the P2 re-derive clears (no code change), or (b) a health-check that is too strict / a missing OIDC var that makes the job error before it can settle. `[lakebase-red-herring]`: the cause is ours.

- [ ] **Step 2: If (b) — a code/config defect:** write the fix red-first (e.g., add the missing `vars.HF_APP_SP_APPLICATION_ID` / OIDC vars to the workflow; or correct the health-check state set with a test asserting `SYNCED_TABLE_ONLINE` during a legitimate in-flight refresh is tolerated with a bounded wait, not a hard fail). Watch it fail, then fix, then pass.

- [ ] **Step 3: If (a) — pure operator settle:** no code change here; the acceptance is verified in P2 (§8 acceptance: both jobs green). Record the diagnosis in the plan-execution log.

- [ ] **Step 4: Record the root cause** (evidence, not theory) before proposing the fix (Failure Investigation Protocol). Report to the owner before implementing.

---

### Task 5: Foundation verification + commit gate

**Files:** none new — full-suite verification of Tasks 1–4.

- [ ] **Step 1: Run all 7 CI gates + pip-audit.**

```bash
uv run ruff check src/ scripts/
uv run ruff format --check src/ scripts/
uv run lint-imports
uv run python scripts/bump_wheel.py --check
uv run python scripts/pip_audit_ignores.py --check
uv run pyright src/ hf_taipy_app/src/ scripts/_tf_env_pins.py scripts/sync_tf_env_pins.py
uv run pytest src/tests/ -q
uv run pip-audit $(uv run python scripts/pip_audit_ignores.py)
```
Expected: all green; capture pytest exit code (not via `| tail`).

- [ ] **Step 2: Confirm no value-changing recompute leaked into P0** — P0 is code-only; `git status` shows only pins/sentinels/elastic/AC-schema/goldens/CI-fix.

- [ ] **Step 3: STOP — request explicit owner approval to commit.** Show the diff / file list. On approval, commit on `feat/sk4118-adoption`:

```bash
git commit -F <foundation commit message>
```
Commit message subject: `chore(sk): adopt silly-kicks 4.120.0 foundation — pins, elastic v2 + B1, CI-fix`. Body enumerates the pin sites, the elastic delete-and-depend, the AC schema +3 columns, and the CI-fix. End with the `Co-Authored-By` trailer.

- [ ] **Step 4: Do NOT push/PR yet** — P1 + P2 land on the same branch; push/PR/merge are their own approval gates after the full cycle is code-complete.

---

## Self-Review

1. **Spec coverage:** §2.3 (elastic mandatory + B1) → Tasks 2/3; §3 (mechanical bump surface) → Task 1; §5.4 B1 → Task 3; §6 step 1 (code adoptions) → Tasks 1–3; §8 (CI-fix) → Task 4. ✓
2. **Placeholders:** Task 4 is intentionally live-gated (diagnosis needs Databricks) — flagged, not a hidden TODO; every other step has exact files/commands. ✓
3. **Type consistency:** `_REQUIRED_SK_MIN` tuple form, `elastic_receive_*` names, `align_events_to_frames`/`ElasticSyncParams` symbols consistent across Tasks 1–3. ✓
