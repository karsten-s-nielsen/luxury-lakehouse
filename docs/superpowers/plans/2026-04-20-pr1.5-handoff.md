# PR 1.5 Handoff Doc — Bronze-Provider-Gaps Cycle

**For the next session picking this up.** Read this + the linked files; then resume from "Remaining tasks" below.

## Context

- **User directive (2026-04-20):** "Bronze is the truth layer — capture every source field. Downstream models progressively surface what's needed. No more re-ingestion unless the source changes." Saved as `feedback_bronze_completeness_principle.md` in memory.
- **PR 1.5 scope** = Option 1 + 2 + 3 from the cross-provider gap audit (see below). Option 4 (StatsBomb `fmt="json"` re-fetch) is **backlog**, not this PR.
- **Merge strategy (user-approved):** PR 1 + PR 1.5 merge to main. PR 2 (passes conformed fact → `match_key`) becomes a **fresh branch off main** in the next cycle. The old `feat/passes-match-key-migration` branch is orphaned — delete when cleaning up.
- **Current branch:** `feat/bronze-provider-gaps` (stacked on PR 1's `feat/tracking-passes-idsse-metrica`).

## PR 1 status to check first

- **SHA on PR 1 branch tip:** `64edfae` (chore(deps): recompile hf_taipy_app/requirements.txt).
- **Before merging PR 1.5**: PR 1's CI must be fully green. Last known state at handoff: Python CI had failed on cachetools/pydantic transitive drift; I pushed a recompile. CI should be green by the time the next session opens. Run `gh pr view 165 --json statusCheckRollup --jq '.statusCheckRollup[] | "\(.workflowName) — \(.conclusion)"'` to confirm.

## Done on PR 1.5 branch (partial, uncommitted)

Only one file modified: `src/ingestion/metrica_common.py`.

1. Added `contextlib` import.
2. Extended `_EPTSMetadata` NamedTuple with 4 new fields:
   - `pitch_length_m: float | None`
   - `pitch_width_m: float | None`
   - `player_id_to_position: dict[str, str]` (raw PlayingPosition string — CB/LB/RM/TW/etc.)
   - `player_id_to_jersey: dict[str, str]` (mirror of `player_id_to_shirt` for downstream clarity)
3. `_parse_epts_metadata` now:
   - Reads `<FieldSize>` element (via `.get("Length")` + fallback to `.findtext("Length")`) → `pitch_length_m`, `pitch_width_m`.
   - Populates `player_id_to_position` from the raw `PlayingPosition` attr (previously read but only used for GK detection).
4. `_parse_epts_events` now:
   - Reads `event.get("to")` and emits as `to` column (**bug fix**: Game 3's `to` was always NULL).
   - Handles `subtypes` as either `dict` (back-compat) or `list[dict]` (multi-subtype events) — emits `subtype` (scalar, first name) AND `subtypes_all_json` (JSON-stringified list of all names).

Linted clean:
- `uv run ruff check src/ingestion/metrica_common.py` → All checks passed
- `uv run ruff format --check` → already formatted
- `uv run pyright` → 0 errors, 0 warnings

## Remaining tasks (PR 1.5 scope)

### Metrica — finish

1. **Propagate pitch dims + new cols to bronze rows.** The parser now captures `pitch_length_m`/`pitch_width_m` in metadata but doesn't yet emit them into tracking/event rows. Need to:
   - Update `_parse_epts_tracking` (in `metrica_common.py`) to include `pitch_length_m` + `pitch_width_m` on each row (denormalized).
   - Check `metrica_tracking.py` `ingest_tracking` — the narrow-format tracking rows for CSV games need these cols as NULL (CSV source has no pitch dims).
   - Similarly for `metrica_events.py` `ingest_events` — new CSV-path events get `to` (already fallthrough) + `subtypes_all_json=NULL` + `pitch_length_m=NULL` + `pitch_width_m=NULL`.
2. **Update `_metrica__sources.yml`** to document the new bronze columns.

### SkillCorner — new

`src/ingestion/skillcorner.py` `_dataset_to_rows()` (~line 93-176). Per the gap analysis, add these kloppy-exposed fields:
- `ball_state` — from `frame.ball_state` (`in_play` / `out_of_play` / `dead_ball`)
- `ball_owning_team` — from `frame.ball_owning_team.team_id` when set
- `ball_z` — from `frame.ball_coordinates.z` (nullable)
- `position_name` — raw `player.starting_position.name` string (currently only derived as GK bool)
- `is_visible` — per-player `ObjectPlayer.is_visible` or equivalent flag
- Also consider: `home_team_id` / `away_team_id` on rows (currently only "home"/"away" strings)

Update `_skillcorner__sources.yml` accordingly.

### IDSSE — full parser rewrite (biggest task)

Current `src/ingestion/idsse.py` `_parse_events_xml` extracts 9 of ~100 DFL fields. See the full DFL attribute enumeration at `/tmp/dfl_enum_full.txt` (saved this session — may not survive, re-run if absent via the enumeration snippet in the conversation history). Summary inventory:

- **Event-level attrs (13):** MatchId, EventId, EventTime, X-Position, Y-Position, X-Source-Position, Y-Source-Position, X-PositionFromTracking, Y-PositionFromTracking, StartFrame, EndFrame, CalculatedFrame, CalculatedTimestamp.
- **31 first-child event types** including: Play, OtherBallAction, TacklingGame, Delete, ThrowIn, BallClaiming, FreeKick, ShotAtGoal, Foul, GoalKick, Substitution, CornerKick, Caution, KickOff, PenaltyNotAwarded, Offside, Nutmeg, RefereeBall, FinalWhistle, SpectacularPlay, VideoAssistantAction, ChanceWithoutShot, Run, PossessionLossBeforeGoal, PlayerNotSentOff, FairPlay, BallDeflection, Penalty, OtherPlayerAction, SitterPrevented, CautionTeamofficial, GoalDisallowed.
- **Nested children attrs**: `Play>Pass` (Direction, OneTwo, FreeKickLayup), `Play>Cross` (Side, GoalKeeper, GoalKeeperInterference), `ShotAtGoal>{Successful|Saved|Wide|Blocked|WoodWork|Other}Shot` each with own attrs.

Schema approach (user-approved Option A): **wide flat bronze schema, ~100-110 columns** with prefixes: event-level (13) + `play_*` (~20) + `shot_*` (~35) + `tackle_*` + `throwin_*` + `corner_*` + `freekick_*` + `goalkick_*` + `kickoff_*` + `foul_*` + `caution_*` + `sub_*` + `offside_*` + `penalty_*` + `var_*` + `delete_*` + etc.

Implementation plan:
1. Rewrite `_parse_events_xml` to walk Event + first-child + nested-child elements, extracting every attribute into a typed col. Use dict-based row building keyed on the schema cols (NULL for non-applicable).
2. Update `_idsse__sources.yml` with every new column doc.
3. The existing `stg_idsse__events.sql` is gated by `pausa_enabled` feature flag — it won't rebuild by default but should still handle the new columns gracefully (cast + pass through).

### Fix `line_breaking.py:78`

Current filter: `event_type IN ('successfulPassEvent', 'failedPassEvent')` — matches nothing.
Correct filter post-IDSSE-rewrite: `event_type = 'Play'` (optionally `AND play_evaluation IN ('successfullyCompleted', 'unsuccessful')` — but for LB detection we want all passes including unsuccessful ones, so the evaluation filter is optional).

### StatsBomb staging expansion

Already-in-bronze, not-in-staging columns to add to `stg_statsbomb__events` and/or `stg_statsbomb__shots`:
- pass_technique, pass_aerial_won, pass_goal_assist, pass_shot_assist, pass_cut_back, pass_deflected, pass_inswinging, pass_outswinging, pass_miscommunication, pass_backheel
- shot_aerial_won, shot_open_goal, shot_redirect, shot_follows_dribble, shot_saved_off_target, shot_saved_to_post, shot_kick_off
- dribble_overrun, dribble_no_touch
- position (player positional role)

JSON-in-bronze, never-parsed:
- `tactics` → extract `tactics.formation` + `tactics.lineup[*]` via `from_json`
- `50_50` → extract `outcome` + `counterpress`
- `shot_freeze_frame` → extract full polygon (list of `{location, teammate, actor, keeper}`) as structured type — currently reduced to count
- `visible_area` (360) → keep full polygon, not just vertex count

Update `_marts__models.yml` / per-model schema docs for dbt contract enforcement.

### Wyscout staging expansion

Decode Wyscout `tags` array IDs into boolean cols in `stg_wyscout__events`:
- 702 → is_head
- 703 → is_right_foot
- 704 → is_left_foot
- 1601 → is_counter_attack
- 503 → under_pressure
- 1702 → is_blocked
- 1401 → is_red_card
- 1402 → is_yellow_card
- 601 → is_sliding_tackle
- 201 → is_opportunity
- 1701 → is_dangerous_ball_lost
- 501 → is_free_space_right
- 502 → is_free_space_left
- 801 → is_high
- 802 → is_low
- 1802 → is_not_accurate (complement of existing `is_accurate`)

Parse `teamsData` JSON from `bronze.wyscout_matches` → new staging model or extended `stg_wyscout__matches` with: formation, score (home/away), starting lineups per team.

Also surface `matchTimestamp`, `subEventId`, `winner`, `gameweek`, `roundId`, `venue`, `date` (local string) at staging layer — all already in bronze.

### Re-ingest

Run ingestion jobs for affected providers:
- IDSSE: `uv run python -m ingestion.idsse` or via Databricks workflow `wf-idsse`
- SkillCorner: `wf-skillcorner` (10 matches)
- Metrica: `wf-metrica` (all 3 games — Game 3 gets the `to` + subtypes fixes; Games 1-2 get the new NULL-for-pitch-dims cols)

Use `scripts/ensure_warehouse.py` wrapper. Warehouse auto-stops after 10 min idle; wrap the dbt/job calls.

### Verify line-breaking

After IDSSE re-ingest + line_breaking.py filter fix:
```bash
uv run python -m ingestion.line_breaking
```
Then:
```sql
SELECT data_source, count(*) FROM soccer_analytics.dev_gold.fct_line_breaking_results GROUP BY data_source
```
Expected: 3 rows — `statsbomb_360`, `metrica_tracking`, `idsse_tracking` (with non-zero count for the last one — finally).

### Doc sweep + TODO

- Update `_<provider>__sources.yml` for every provider touched.
- Add new TODO.md tech-debt entry for **Option 4**: "StatsBomb team IDs + manager IDs via `fmt='json'` re-fetch on `statsbomb_matches` ingestion; currently statsbombpy flattens team objects to name strings, losing `home_team_id`/`away_team_id`/`home_team.country`/`home_managers[].id`/etc. Re-ingest-once-eliminated per bronze-completeness principle."
- Update Kimball Migration table in TODO.md: PR 1.5 shipped; PR 2 now branches from main fresh.
- Remove orphaned `feat/passes-match-key-migration` branch (`git branch -D` after confirming PR 2 plan file survives on main).

## Task list

See `TaskList` in the tool — 10 tasks numbered #13-22 covering Metrica (partial), SkillCorner, IDSSE, line-breaking fix, StatsBomb staging, Wyscout staging, re-ingest, verify, docs, commit.

## Key files

- `src/ingestion/metrica_common.py` (modified, uncommitted)
- `src/ingestion/metrica_events.py`, `src/ingestion/metrica_tracking.py` (need pitch-dim propagation)
- `src/ingestion/skillcorner.py` (to modify)
- `src/ingestion/idsse.py` (full `_parse_events_xml` rewrite — biggest change)
- `src/ingestion/line_breaking.py:78` (filter fix)
- `dbt_project/models/staging/statsbomb/stg_statsbomb__events.sql`, `stg_statsbomb__shots.sql` (expand)
- `dbt_project/models/staging/wyscout/stg_wyscout__events.sql`, `stg_wyscout__matches.sql` (expand)
- `dbt_project/models/staging/{metrica,idsse,skillcorner}/_*__sources.yml` (doc every new col)
- `dbt_project/models/marts/_marts__models.yml` (dbt contract updates if any mart column widens)
- `TODO.md` (add Option 4 backlog entry; update Kimball Migration table on PR 1.5 ship)

## Gotchas

- **CRLF warning** on metrica_common.py on commit — ignore. Pre-existing in this repo on Windows.
- **Don't use subagent ceremony** — user fatigue on dual-review cycles for mechanical tasks. Single implementer + automated gates (ruff + pyright + pytest) is the approval for mechanical work. Reserve reviewer subagents for ADR-level changes.
- **Bash>30s must use `run_in_background=true`** — enforced hook. Use the `until grep -qE ...; do sleep 15; done` polling pattern.
- **`databricks-sql-connector` is local-dev-only** — use `uv run --with databricks-sql-connector python ...` for live queries. `DATABRICKS_HOST` has `https://` prefix — strip before passing to `sql.connect`.
- **Metrica CSV-path quirk** — `To` column name maps to `to` via generic sanitize (lowercasing). No explicit handling needed in CSV path; my `to` fix was only for EPTS.
- **User rule** — `single commit per branch` is the merge convention (squash-merge to main). Work-in-progress commits on the feature branch are OK; the squash collapses them at merge time.
- **User rule** — `no commits / no push / no PR without explicit approval`. The Metrica-partial commit about to land on the branch is the WIP checkpoint approved via "2 is fine"; the final PR 1.5 open + merge each need separate approval.
