# silly-kicks 4.118.0 Adoption — P1 New Metrics Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Materialize the eight new silly-kicks 4.118.0 metric families in the lakehouse — each as a full ADR-013 chain (Python writer → bronze → dbt staging view → gold mart with `contract: enforced: true`) plus governance, academic references, and UI — grain-bucketed into shared wide marts.

**Architecture:** Every new event-only metric mirrors the canonical `src/ingestion/bravery_writer.py` ADR-013 template: a module-level `_REQUIRED_SK_MIN`, a `*_METRIC_COLUMNS` tuple mirroring the sk package constant, a pure unit-tested `_compute_*_group`, an `applyInPandas` `run_pipeline` writing bronze with `replaceWhere`, and a `main()` gated by `require_sk`. Writers emit NATIVE ids; surrogate keys resolve in the mart via INNER JOIN on `dim_*`. Tracking metrics (gk_decision reconstruction tier, restdefense) run in the tracking-marts drain. Marts are wide-by-granularity (§4).

**Tech Stack:** Python 3.10, PySpark (Databricks serverless, `applyInPandas`), dbt (databricks, contracts), silly-kicks 4.118.0, Taipy (hf_taipy_app), HuggingFace Hub (ADR-072 guarded seam).

**Spec:** `docs/superpowers/specs/2026-09-17-silly-kicks-4118-adoption-design.md` (§4, §5, §7). Depends on **P0** (branch `feat/sk4118-adoption` already on 4.118.0). Executes on the same branch.

## Global Constraints

- Python 3.10; line length 120. silly-kicks pinned `==4.120.0` (P0; contains SK-EXPORT 4.119.0 + packing-fix 4.119.1 + TF-54b Path B 4.120.0). All new writers declare `_REQUIRED_SK_MIN = (4, 120, 0)`.
- **ADR-013:** Python writer → bronze raw (`_ingested_at` UTC audit col) → dbt staging view → gold mart `contract: enforced: true`. Writers emit only native ids + values; surrogate keys (`match_key`, `team_key`, `player_key`, `competition_key`) resolve in the mart via INNER JOIN on the identity fact / `dim_*`.
- **Native ids** via `src/shared/identifiers.py`; `<entity>_native` for provider strings paired with Kimball BIGINT surrogates. Feed sk `compute_*` the NATIVE `team_id_native` / `player_id_native` / `match_id_native`, never the hashed surrogate.
- **access_tier (ADR-064):** every new bronze writer stamps `access_tier` per row (direct stamp from the source `visibility`, never a publish-time join). Publishers go through `ingestion.hf_upload_seam.prepare_public_upload` (ADR-072) — never direct `HfApi`.
- **Schema-drift guard (ADR-002 §4 idiom) — the released `METRIC_CONTRACTS` registry, ONE generic loop (SK-EXPORT / sk 4.119.0, sk:ADR-098).** Do NOT import per-package `*_METRIC_COLUMNS`/`*_KEYS` individually. Import the uniform registry and loop with zero per-package name/type knowledge:
  ```python
  from silly_kicks.metric_contracts import METRIC_CONTRACTS

  for family, c in METRIC_CONTRACTS.items():  # 7 families
      assert set(mart_columns(family)) == set(c["metric_columns"]) | set(c["keys"])
  ```
  Each `MetricContract` carries `keys` / `metric_columns` / `columns` (all `tuple[str,...]`) + `column_types: Mapping[str,str] | None`. The lakehouse parity test asserts each new mart's column set equals `set(metric_columns) | set(keys)` for its family. The registry values are sk-side round-trip-verified against the package constants (sk `tests/test_metric_contracts.py`), so they cannot drift.
  - **`column_types` for the DDL:** non-`None` for the 5 typed families (`team_metrics`, `match_outcome`, `shot_stopping`, `territory`, `duels`) → use it as the authoritative mart column dtypes; `None` for `gk_decision` + `restdefense` → the lakehouse infers those (OI-3).
  - **VAEP variant (xsuccess/VAEP_adjusted)** is NOT in `METRIC_CONTRACTS` (`xsuccess` is `_EXEMPT` sk-side) — its guard asserts the EXISTING `fct_action_values` VAEP columns are byte-identical (additive-only) + the 2 new columns present.
- **Grain keys — all from `METRIC_CONTRACTS[family]["keys"]`.** Note `gk_decision` keys are `("game_id", "keeper")` (NOT `player_id` — sk emits `keeper`/`keeper_raw`); the `fct_gk_decision` mart keys on `keeper` and resolves it to `player_key`.
- **DEPENDENCY — RESOLVED, pin `==4.120.0`:** the cycle now targets silly-kicks **4.120.0** (owner: include the 4.119.1 packing fix + 4.120.0 TF-54b Path B). Additive over 4.118.0 — the 8 metric families (spec §2, shipped 4.107–4.118) + the `metric_contracts` registry (4.119.0) + the `secured_reception` packing fix (4.119.1, unblocks the AC add_packing on non-chronological bronze) + the `territory` counterfactual method (4.120.0) are all present in 4.120.0.
- **NEW metric surface (4.120.0, sk:ADR-099) — the `territory` counterfactual "threat-prevented" method** (owner: "adding any new metrics"). `territory` (metric #5, Task C1) materializes BOTH methods: `completed_failed` (default, byte-identical to the pre-4.120 output) AND `counterfactual` (`compute_territorial_dominance(method="counterfactual")`) — +4 cf metric cols + `territory_target_source` provenance on `fct_player_match_metrics` (same player-match grain). Injects `xt` (already) + `PassCompletionModel.bundled()` (4.120.0 full-open-data re-fit). **Ranking-LICENSED** per the sk defender-ranking census (ICC lo=0.121>0, power=1.0) — governance/UI may present a per-defender ranking (distinct from gk_decision's instrument-not-ranking limit). **[SPEC-R5-01 — corrects the spec-r4 "12→16" error] `TERRITORY_METRIC_COLUMNS` = 12, and the counterfactual columns are METHOD-CONDITIONAL, NOT in the registry** (verified vs installed sk 4.120.0): `TERRITORY_METRIC_COLUMNS` = 12; `METRIC_CONTRACTS["territory"].metric_columns` = 12 + `keys` = 2 (`game_id`,`player_id`); `columns_for_method("completed_failed")` = 15 (2 keys + 12 metric + `territory_hull_source` provenance); `columns_for_method("counterfactual")` = 20 = the 15 + **5 counterfactual-only** cols: 4 metric (`territory_expected_threat_faced`, `territory_xt_prevented_above_expectation`, `territory_passes_aimed_into_hull` [Int64], `territory_mean_completion_faced`) + `territory_target_source` (provenance). There is NO public `_COUNTERFACTUAL_ONLY_COLUMNS`; the cf surface is obtained by set-diff of `columns_for_method("counterfactual") − columns_for_method("completed_failed")`. **Guarding (two contracts):** the v1 slice is registry-guarded — the `METRIC_CONTRACTS["territory"]` parity asserts the mart's v1 metric surface == the 12 (+2 keys); the cf slice is guarded SEPARATELY against `columns_for_method("counterfactual")` (sk owns those names + dtypes), asserted only when the counterfactual method runs. The registry parity NEVER expects 16 — that number was wrong. **Hyrum (transparent here):** the 4.120.0 `PassCompletionModel.bundled()` re-fit shifts gk_decision's reconstruction-tier values, but gk_decision is NEW this cycle → it uses the 4.120.0 bundle from the start (no re-materialize). **[SPEC-R4-02] Any cited PassCompletionModel correlation is a PRE-4.120 measure** — TF-62's ρ≈0.24–0.25 (WC2022, AUC 0.811) predates the 4.120.0 full-open-data re-fit (all-comp AUC 0.794); re-state it as "pre-4.120" and re-measure against the 4.120.0 bundle before any UI/NOTICE/model-card display of a territory/gk_decision correlation.
- **Every displayed value carries scale + direction** (UI): 0–1 ranges, higher/lower = better, in axis/title/tooltip/caption. No raw IDs. Goal-oriented nav labels.
- **NOTICE + GLOSSARY + PAGE_TERMS + `_marts__models.yml`** updated in the SAME change as the mart/page (never a follow-up). Git-scope any completeness grep (avoid the `.venv` vendored-dbt trap).
- Approval gates: no commit/push/PR/operator-run/merge without separate explicit owner "yes". Minimal fully-tested commits (one coherent commit per metric family is acceptable; do NOT micro-commit per file).
- ADR refs lakehouse-default; sk-series marked `sk:ADR-NNN`.

---

## File Structure (per metric — the ADR-013 slice)

For each metric `M` with entry-point name `m_writer`:

- **Writer:** `src/ingestion/<m>_writer.py` (mirror `bravery_writer.py`).
- **Entry point:** `pyproject.toml` `[project.scripts]` → `<m>_writer = "ingestion.<m>_writer:main"`.
- **Bronze table:** `soccer_analytics.bronze.<m>` + an idempotent `scripts/migrations/2026-09-17-add-<m>.sql` (`CREATE TABLE IF NOT EXISTS` with the DDL) — operator-applied WITH the merge.
- **dbt staging:** `dbt_project/models/staging/<m>/stg_<m>.sql` + `_<m>__sources.yml`.
- **dbt mart:** the grain-bucketed target (§4) + entry in `dbt_project/models/marts/_marts__models.yml` with `contract: enforced: true` + column tests.
- **Workflow card:** `workflow-cards/wf-<m>.yaml` (phase-parity, `dbt_model:`, environment matching the TF task, `references:`, and `governance:` block if per-player-evaluative).
- **TF task:** a `databricks_job` task block in `terraform/modules/workflows/main.tf` (mirror the `bravery_writer` task at ~:189; env = `analytics` or `wheel` per what it imports).
- **Seed:** a row in `dbt_project/seeds/task_workflow_mapping.csv` (`<m>_writer,wf-<m>`).
- **Governance (if per-player-evaluative):** add `wf-<m>` to `PER_PLAYER_EVALUATIVE_CARDS` + `WORKFLOW_TO_MODEL_CARD` in `src/tests/test_ai_governance_md.py`; create `docs/huggingface/model-cards/<m>.md` with the EU-AI-Act stanza + SEC-AUDIT provenance tag; add the `AI_GOVERNANCE.md` §5 inventory entry.
- **References:** add authors to `ARCHITECTURE.md` §8 Appendix D + `expected_authors` in `src/tests/test_architecture_md_appendix.py`; add the `NOTICE` attribution.
- **UI:** a Taipy page per `hf_taipy_app/CLAUDE.md` (state module + page config + `main.py` registry + `template.py` PAGE_TERMS + GLOSSARY).
- **Tests:** `src/tests/test_<m>_writer.py` (pure `_compute_*_group` on fixtures + schema/census/canonical-id), the mart-column parity test, format-contract test if a new join key, `_marts__models.yml` completeness.

The invariant writer skeleton (const block → pure `_compute_*_group` → `applyInPandas run_pipeline` with `replaceWhere` → `main()` with `require_sk` + `argparse`) is **copied from `bravery_writer.py`**; per-metric tasks below specify only what differs (sk call, grain keys, columns, injected model, mart).

---

## Phase A — team-match mart `fct_team_metrics` (team_metrics + match_outcome)

### Task A1: team_metrics writer + bronze

**Files:** Create `src/ingestion/team_metrics_writer.py`, `src/tests/test_team_metrics_writer.py`, `scripts/migrations/2026-09-17-add-team-metrics.sql`. Modify `pyproject.toml` (entry point).

**Interfaces:**
- Consumes: `silly_kicks.team_metrics.compute_team_kpis(actions, *, xg_column=None, params=TeamKpiParams()) -> (samples, TeamKpiReport)`; keys `TEAM_KPI_KEYS` (`game_id`, `team_id`); metric columns `TEAM_KPI_METRIC_COLUMNS` (44 cols across PRESSING/PROGRESSION/BUILDUP/POST_RECOVERY).
- Produces: `bronze.team_metrics` grain `(data_source, match_id, team_id)` native + 44 metric cols + `access_tier` + `_ingested_at`.

- [ ] **Step 1: Write the failing pure-core test.** `test_team_metrics_writer.py`: feed a two-team fixture of `bronze.spadl_actions` rows through `_compute_team_kpis_group`; assert output has `TEAM_KPI_METRIC_COLUMNS` (import the sk constant), one row per team, and the `TeamKpiReport` conservation census (`n_scored + n_excluded == n_in`).

```python
from silly_kicks.team_metrics import TEAM_KPI_METRIC_COLUMNS


def test_team_kpis_group_shape(two_team_actions):
    from ingestion.team_metrics_writer import _compute_team_kpis_group

    out = _compute_team_kpis_group(two_team_actions)
    assert set(TEAM_KPI_METRIC_COLUMNS).issubset(out.columns)
    assert len(out) == 2  # one row per team
```

- [ ] **Step 2: Run it (fail).** `uv run pytest src/tests/test_team_metrics_writer.py -q` → FAIL (module absent).

- [ ] **Step 3: Write the writer** by copying `bravery_writer.py` and specializing: `BRONZE_TABLE = "team_metrics"`; read `_SPADL_READ_COLUMNS` = the sk-required projection (introspect `compute_team_kpis`; at minimum `type_id, team_id_native, game_id, x, y, end_x, end_y, time_seconds, result_id, bodypart_id, possession_id` + optional `xg`); `_<...>_METRIC_COLUMNS = tuple(TEAM_KPI_METRIC_COLUMNS)`; pure `_compute_team_kpis_group` calls `compute_team_kpis(actions, xg_column=None)[0]`; stamp native `team_id`/`match_id` + `access_tier`; `run_pipeline` dispatches per-match via `applyInPandas`, `write_delta_table(..., mode replaceWhere match_id, row_count=...)`.

- [ ] **Step 4: Run the pure-core test (pass).** `uv run pytest src/tests/test_team_metrics_writer.py -q` → PASS.

- [ ] **Step 5: Add the entry point** to `pyproject.toml` + the bronze migration SQL (`CREATE TABLE IF NOT EXISTS soccer_analytics.bronze.team_metrics (...DDL... , access_tier STRING, _ingested_at TIMESTAMP)`).

- [ ] **Step 6: Add the mart-column parity test** (`test_team_metrics_mart_matches_sk_columns`) asserting the writer `_OUTPUT_TYPES` keys == `TEAM_KPI_METRIC_COLUMNS` + keys. Watch it fail on a deliberate drift, then pass.

### Task A2: match_outcome writer + bronze

**Files:** Create `src/ingestion/match_outcome_writer.py`, `src/tests/test_match_outcome_writer.py`, `scripts/migrations/2026-09-17-add-match-outcome.sql`. Modify `pyproject.toml`.

**Interfaces:**
- Consumes: `silly_kicks.match_outcome.compute_match_outcome(actions, *, xg_column, params=MatchOutcomeParams()) -> (samples, MatchOutcomeReport)`; keys `MATCH_OUTCOME_KEYS` (`game_id`, `team_id`) + metric cols `MATCH_OUTCOME_METRIC_COLUMNS` (5: p_win/p_draw/p_loss, xpoints, expected_goals) — **public via SK-EXPORT** (parity guard imports them). Injects per-shot xG. `MatchOutcomeParams()` default = both corrections ON (sk:ADR-097).
- Produces: `bronze.match_outcome` grain `(data_source, match_id, team_id)` + 5 cols + `access_tier`.

- [ ] **Step 1: Resolve the xG injection source.** The lakehouse serves pre-shot xG via `xg_model_v3` (`fct_xg_predictions_v2` / `ingestion.xg_shot_scorer`). The writer reads the gold action corpus joined to the xG per shot; feed `compute_match_outcome(actions, xg_column="xg")`. Write the failing test asserting a two-team match yields a valid simplex (`p_win+p_draw+p_loss≈1`) and `xpoints == 3*p_win + p_draw`.

- [ ] **Step 2: Run it (fail).**

- [ ] **Step 3: Write the writer** (copy bravery; `BRONZE_TABLE="match_outcome"`; join xG per shot; `MatchOutcomeParams()` default = both corrections ON per sk:ADR-097; stamp native ids + access_tier; per-match `applyInPandas`).

- [ ] **Step 4: Run the test (pass).**

- [ ] **Step 5: Entry point + migration + parity test** (as A1 Step 5–6).

### Task A3: `fct_team_metrics` mart + staging + wiring

**Files:** Create `dbt_project/models/staging/team_metrics/{stg_team_metrics.sql,_team_metrics__sources.yml}`, `dbt_project/models/staging/match_outcome/{stg_match_outcome.sql,_match_outcome__sources.yml}`, `dbt_project/models/marts/fct_team_metrics.sql`. Modify `dbt_project/models/marts/_marts__models.yml` (contract), `workflow-cards/{wf-team-metrics,wf-match-outcome}.yaml`, `terraform/modules/workflows/main.tf` (2 tasks), `dbt_project/seeds/task_workflow_mapping.csv` (2 rows).

- [ ] **Step 1:** Write `stg_team_metrics` / `stg_match_outcome` views selecting from the bronze tables (dedup latest `_ingested_at` per grain if the writer appends).
- [ ] **Step 2:** Write `fct_team_metrics.sql` — grain `(match_key, team_key)`, INNER JOIN `dim_matches` on native `match_id` + `dim_teams` on `(provider, native_team_id)`; carry the 44 team_metrics cols + 5 match_outcome cols + `competition_key`, `season_id`, `data_source`, `access_tier`, surrogate `team_metrics_id`.
- [ ] **Step 3:** Add the `_marts__models.yml` entry with `contract: enforced: true`, column types, and not-null tests on the keys. Run the `_marts__models.yml` completeness test (git-scoped) → PASS.
- [ ] **Step 4:** Cards + TF tasks + seed rows (mirror `wf-bravery` / the `bravery_writer` TF task). team_metrics/match_outcome are per-team, **NOT** per-player-evaluative → NO `PER_PLAYER_EVALUATIVE_CARDS` entry (assert this against the test's definition; add a code comment stating the exclusion rationale).
- [ ] **Step 5:** `NOTICE` + `ARCHITECTURE.md` §8 (Dixon & Coles 1997; Sumpter/Twelve.football) + `expected_authors`. dbt build + contract test locally where possible; full pytest for the parity/governance/appendix tests.

---

## Phase B — keeper-match marts (shot_stopping supersession + gk_decision)

### Task B1: shot_stopping writer + 3-mart supersession

**Files:** Create `src/ingestion/shot_stopping_writer.py`, `src/tests/test_shot_stopping_writer.py`, `scripts/migrations/2026-09-17-add-shot-stopping.sql`, `dbt_project/models/staging/shot_stopping/{stg_shot_stopping.sql,_shot_stopping__sources.yml}`. Modify `dbt_project/models/marts/fct_gk_shot_stopping.sql`, `fct_gk_shot_stopping_pooled.sql`, `fct_goalkeeper_stats.sql` (re-source the GSAA/psxg columns from the sk output; fix the `_pooled` NULL-season IDSSE join with `<=>`), `_marts__models.yml`, `pyproject.toml`, cards/TF/seed.

**Interfaces:**
- Consumes: `silly_kicks.shot_stopping.compute_shot_stopping(actions, *, psxg_column, defending_gk_column="defending_gk_player_id", defending_team_column="defending_gk_team_id", params) -> (samples, ShotStoppingReport)`; keys `SS_KEYS` (`game_id`, keeper); metric cols `SHOT_STOPPING_METRIC_COLUMNS` (8: shots_faced, goals_conceded, psxg_faced, goals_prevented + `_excl_penalties` companions).
- Produces: `bronze.shot_stopping` grain `(data_source, match_id, keeper_id, keeper_team_id)` native + 8 cols + `access_tier`. Re-sources `fct_gk_shot_stopping(_pooled)` + the `fct_goalkeeper_stats` psxg CTE.

- [ ] **Step 1: Reconcile decision confirmed (spec §5.3):** supersede the inline SQL GSAA rollup across all three GK marts with the sk writer's canonical-id-grouped output. Write the failing test: `_compute_shot_stopping_group` over a keeper-with-mixed-dtype-id fixture yields ONE row per canonical keeper (not fragmented) with `goals_prevented == psxg_faced - goals_conceded` and the `ShotStoppingReport` census conserving.

- [ ] **Step 2: Run it (fail).**

- [ ] **Step 3: Write the writer** (copy bravery; inject PSxG from `fct_shot_psxg`/the psxg column; feed `compute_shot_stopping(actions, psxg_column="psxg_recalibrated")`; group canonical-id, emit raw). Stamp access_tier.

- [ ] **Step 4: Run the test (pass).**

- [ ] **Step 5: Re-source the marts.** Rewrite `fct_gk_shot_stopping.sql` to select from `stg_shot_stopping` (native → `player_key`/`match_key` via `dim_*`) instead of the inline `fct_shot_psxg` rollup; propagate to `_pooled` (fix the NULL-season join: `season_id <=> ...`); replace the `psxg_agg` CTE in `fct_goalkeeper_stats.sql` with a join to the new staging. Keep the mart column names/shape (Hyrum) — assert byte-compatible column sets with a test.

- [ ] **Step 6: Governance — `wf-shot-stopping` is per-player-evaluative:** add to `PER_PLAYER_EVALUATIVE_CARDS` + `WORKFLOW_TO_MODEL_CARD` (`docs/huggingface/model-cards/shot-stopping.md`); `AI_GOVERNANCE.md` §5; the model card's EU-AI-Act + honest-limit stanza (GSAA is low-sample per match; the pooled layer is the evaluative surface). `NOTICE` + Appendix D (StatsBomb PSxG lineage). Re-run `test_ai_governance_md.py`, `test_architecture_md_appendix.py`.

- [ ] **Step 7: Entry point + migration + cards + TF + seed + parity test.**

### Task B2: gk_decision writer (native + reconstruction tiers)

**Files:** Create `src/ingestion/gk_decision_writer.py`, `src/tests/test_gk_decision_writer.py`, `scripts/migrations/2026-09-17-add-gk-decision.sql`, `dbt_project/models/staging/gk_decision/{stg_gk_decision.sql,_gk_decision__sources.yml}`. Modify the keeper-match mart (extend `fct_goalkeeper_stats` or a new `fct_gk_decision.sql` — decide from the column count; leaning a new `fct_gk_decision` to avoid over-widening goalkeeper_stats), `_marts__models.yml`, `pyproject.toml`, cards/TF/seed, governance.

**Interfaces:**
- Consumes: `silly_kicks.gk_decision.compute_gk_decision_value(option_set: OptionSet, *, params=GkDecisionParams(), extra_drops=None) -> (samples, GkDecisionReport)` + the tier adapters `SkillCornerGIOptionSet` (native GI) and `ReconstructedOptionSet` (SB360/full-tracking, reachability default 0.85, uses the bundled `PassCompletionModel`). Sample output: **5 metric cols** `GK_DECISION_METRIC_COLUMNS` (`decision_value`, `sel_efficiency`, `decision_pct`, `chosen_ev`, `best_ev`) **+ 2 non-metric provenance cols** `GK_DECISION_SAMPLE_COLUMNS`-tail (`n_options`, `option_set_source`) — **public via SK-EXPORT** (parity guard imports them). Grain `["game_id","keeper"]` (from `GK_DECISION_KEYS`; sk emits `keeper`/`keeper_raw`, NOT `player_id`).
- Produces: `bronze.gk_decision` grain `(data_source, match_id, keeper_id)` native + metric cols + `option_set_source` provenance + `access_tier`.

- [ ] **Step 1: Tier routing.** Write the failing test for BOTH tiers: native tier from a SkillCorner GI passing-option fixture (`SkillCornerGIOptionSet`), reconstruction tier from an SB360 freeze-frame fixture (`ReconstructedOptionSet` + bundled PassCompletion). Assert `decision_pct` in [0,1], random ⇒ ~0.5, and the `GkDecisionReport` conserves drops (`too_few_options`/`no_unique_chosen`/`chosen_unvalued` + reconstruction `no_frame`/`fov_cropped`).

- [ ] **Step 2: Run it (fail).**

- [ ] **Step 3: Write the writer** — this is a TRACKING-consuming metric (reconstruction tier reads frames), so it runs in the tracking-marts drain path (P2), NOT a pure event `applyInPandas`. The writer builds the provider-appropriate `OptionSet` per unit (SkillCorner GI rows → native; SB360/full-tracking frames → reconstruction), calls `compute_gk_decision_value`, stamps native keeper id + `option_set_source` + access_tier.

- [ ] **Step 4: Run the test (pass).**

- [ ] **Step 5: Mart + wiring + governance.** `fct_gk_decision` mart (keeper-match grain). `wf-gk-decision` per-player-evaluative → governance (model card carries the HONEST LIMIT: instrument, NOT a ranking; reconstruction ρ≈0.24–0.25 moderate — **[SPEC-R5-03] cite as a PRE-4.120 measure** (WC2022 PassCompletion bundle, AUC 0.811); the 4.120.0 all-comp re-fit (AUC 0.794) is the bundle this cycle ships, so re-measure the correlation against it before printing a number on the card). `NOTICE` + Appendix D (Eyestone collaboration; Power et al. 2017 xPass). Parity + census tests.

---

## Phase C — player-match mart `fct_player_match_metrics` (territory + duels)

### Task C1: territory writer + bronze

**Files:** Create `src/ingestion/territory_writer.py`, `src/tests/test_territory_writer.py`, `scripts/migrations/2026-09-17-add-territory.sql`. Modify `pyproject.toml`.

**Interfaces:**
- Consumes: `silly_kicks.territory.compute_territorial_dominance(actions, *, xt: ExpectedThreat, method="completed_failed"|"counterfactual", window=None, params=TerritoryParams(...), completion_model: PassCompletionModel | None = None, cf_params: CounterfactualParams | None = None) -> (samples, TerritoryReport)`; keys `TERRITORY_KEYS` (`game_id`, `player_id`, public); v1 metric cols `TERRITORY_METRIC_COLUMNS` (12, public); the cf column set via `territory.columns_for_method("counterfactual")` (20 = keys+12+`territory_hull_source`+the 5 cf-only). `method="counterfactual"` (TF-54b) **requires** an injected `completion_model` (`silly_kicks.expected_passing.PassCompletionModel.bundled()` — no-arg fitted classmethod) + optional `cf_params=CounterfactualParams(...)` (from `silly_kicks.territory`). Injects a fitted `ExpectedThreat` for both methods.
- Produces: `bronze.territory` grain `(data_source, match_id, player_id)` native + the **20-col** counterfactual union (12 v1 metric + `territory_hull_source` + 4 cf metric + `territory_target_source`) + `access_tier`. The v1 12 are always populated; the 5 cf-only are populated by the counterfactual pass (present-but-NULL only if a match cannot run counterfactual — but the bounded IDSSE/tracking corpus runs both).

- [ ] **Step 1: Resolve the xT injection + the completion model.** Load/fit the `ExpectedThreat` once (loop-invariant; broadcast the fitted object into the `applyInPandas` closure via a frozen dataclass) — the lakehouse canonical xT (post ExT-v2, or sk xthreat). Also construct `PassCompletionModel.bundled()` once and broadcast it the same way (needed only by the counterfactual pass). Write the failing test: a defender-with-hull fixture yields finite `territory_xt_conceded/prevented` (v1) AND finite `territory_expected_threat_faced` + `territory_xt_prevented_above_expectation` (cf); a <3-action defender → `territory_hull_source="degenerate"` NaN row, counted; assert the produced column set == `set(columns_for_method("counterfactual"))` minus keys.

- [ ] **Step 2–5:** Run-fail → writer (copy bravery; per-match `applyInPandas`; inject xt): run **BOTH** methods per group — `compute_territorial_dominance(actions, xt=xt)` (v1, 15) then `compute_territorial_dominance(actions, xt=xt, method="counterfactual", completion_model=pcm, cf_params=CounterfactualParams(...))` (20), and LEFT-merge the 5 cf-only cols onto the v1 samples on `TERRITORY_KEYS` → the 20-col bronze row → run-pass → entry point + migration (bronze.territory carries all 20) + parity test. `wf-territory` per-player-evaluative → governance (model card; the known under-count limit + the [SPEC-R4-02] pre-4.120 caveat on any cited correlation). `NOTICE` + Appendix D (Sumpter/Twelve 'Earpiece').

### Task C2: duels writer + bronze (STATEFUL Glicko-2)

**Files:** Create `src/ingestion/duels_writer.py`, `src/tests/test_duels_writer.py`, `scripts/migrations/2026-09-17-add-duels.sql`. Modify `pyproject.toml`.

**Interfaces:**
- Consumes: `silly_kicks.duels.compute_duel_ratings(actions, *, initial_ratings=None, window=None, window_stat="as_of_end", params) -> (samples, DuelRatingReport)`; keys `DUEL_KEYS` (`game_id`, `player_id`, public via SK-EXPORT); metric cols `DUEL_METRIC_COLUMNS` (already public). Glicko-2 is **stateful/chronological** — ratings carry forward in ascending `game_id`; `DuelRatingReport.final_ratings` is the resume seed.
- Produces: `bronze.duels` grain `(data_source, match_id, player_id)` native + rating/deviation/volatility + contested/won/lost + `duel_winner_source` + `access_tier`.

- [ ] **Step 1: DECISION — full-corpus recompute (spec §11 open, resolve here):** because the corpus is bounded and existing-only, process the FULL corpus in one ordered pass (`compute_duel_ratings` over all matches ascending `game_id`), NOT an incremental resume — simpler and deterministic. This means the duels writer is a **single-driver ordered pass**, NOT a per-match `applyInPandas` (order matters; `applyInPandas` groups are unordered). Document this deviation from the bravery template explicitly. Write the failing test asserting resume-equivalence is NOT relied on and a two-match ordered pass matches the sk worked-example ratings.

- [ ] **Step 2–5:** Run-fail → writer (ordered driver pass; feed native `player_id`/`team` for the derived-winner adjacency) → run-pass → entry point + migration + parity test. `wf-duels` per-player-evaluative → governance (model card; ground-duels-only + derived-adjacency ~11/match limit). `NOTICE` + Appendix D (Glickman, Glicko-2).

### Task C3: `fct_player_match_metrics` mart + staging + wiring

**Files:** Create `dbt_project/models/staging/{territory,duels}/…`, `dbt_project/models/marts/fct_player_match_metrics.sql`. Modify `_marts__models.yml`, cards/TF/seed.

- [ ] **Step 1–3:** staging views → `fct_player_match_metrics.sql` grain `(match_key, player_key)` INNER JOIN `dim_matches` + `dim_players` on native ids; carry territory (the **20-col** counterfactual union = 12 v1 metric + `territory_hull_source` + 4 cf metric + `territory_target_source`) + duels cols + `data_source`/`access_tier`/surrogate. `_marts__models.yml` contract declares all 20 territory cols with the sk dtypes (`territory_passes_aimed_into_hull` is `Int64`→bigint, `territory_target_source` string, rest double). **Two-contract parity test:** the v1 slice asserts equality to `set(METRIC_CONTRACTS["territory"].metric_columns) | set(keys)` (12+2); the cf slice asserts the mart carries exactly `columns_for_method("counterfactual") − columns_for_method("completed_failed")` (the 5 cf-only) — NEVER fold the cf cols into the registry-12 assertion. Completeness (git-scoped). Cards + TF + seed.

---

## Phase D — VAEP variant (xSuccess + VAEP_adjusted) on `fct_action_values`

### Task D1: xSuccess + VAEP_adjusted columns

**Files:** Modify `src/ingestion/` VAEP writer (the compute_spadl_vaep path) or add `src/ingestion/vaep_adjusted_writer.py`; `dbt_project/models/marts/fct_action_values.sql` (+ columns); `_marts__models.yml`; `src/tests/test_vaep_adjusted_writer.py`; `pyproject.toml` if a new entry point.

**Interfaces:**
- Consumes: `silly_kicks.xsuccess.XSuccessModel` (bundled weights) + `silly_kicks.vaep.VAEP.rate_adjusted(...)` / `silly_kicks.vaep.adjusted.adjusted_value(actions, p_scores_success, p_concedes_fail, xsuccess)`. VAEP_adjusted re-scores an already-fitted STANDARD VAEP on a surgical result-feature counterfactual weighted by xSuccess — **no retrain**.
- Produces: new columns on `fct_action_values` — `xsuccess`, `vaep_adjusted_value` (+ offensive/defensive splits if the sk API exposes them) — additive; existing VAEP columns unchanged.

- [ ] **Step 1:** Write the failing test: on a `HybridVAEP`-style fixture `rate_adjusted` raises (non-vacuity guard); on a result-bearing VAEP the adjusted value differs from raw where xSuccess≠1; NaN xSuccess → NaN adjusted (never fabricated).

- [ ] **Step 2:** Run-fail.

- [ ] **Step 3:** Implement: load the bundled `XSuccessModel`, compute `xsuccess` per action, call `rate_adjusted` on the existing fitted VAEP, emit the additive columns. Additive — assert the existing VAEP columns are byte-identical (parity test).

- [ ] **Step 4:** Run-pass.

- [ ] **Step 5:** Extend `fct_action_values` + `_marts__models.yml` contract; `wf-vaep` already per-player-evaluative (no new card, but note the variant in the model card). `NOTICE` + Appendix D (Paul, Klemp & Memmert 2025; Anzer–Bauer; XGBoost Chen & Guestrin 2016).

---

## Phase E — restdefense tracking mart (L1 + L2)

### Task E1: restdefense writer (tracking-consuming)

**Files:** Create `src/ingestion/restdefense_writer.py`, `src/tests/test_restdefense_writer.py`, `scripts/migrations/2026-09-17-add-restdefense.sql`, `dbt_project/models/staging/restdefense/…`, `dbt_project/models/marts/fct_rest_defense.sql`. Modify `_marts__models.yml`, `pyproject.toml`, cards/TF/seed.

**Interfaces:**
- Consumes: `silly_kicks.restdefense.compute_rest_defense(actions, frames, *, xt=None, goal_map=None, ..., params) -> (samples, RestDefenseReport)`; sample keys `RD_SAMPLE_KEYS` (`game_id, period_id, team_id, action_id`); cols `RD_METRIC_COLUMNS` (`RD_LAYER1_COLUMNS` 11 + `RD_LAYER2_COLUMNS` 5). L2 gated on a fitted `xt` (NaN without it). Tracking-consuming.
- Produces: `bronze.rest_defense` at `RD_SAMPLE_KEYS` grain native + 16 cols + `rd_geometry_source` + FOV companions + `access_tier`.

- [ ] **Step 1:** Write the failing test: L1-only (no xt) yields the 11 descriptive cols with L2 all-NaN (byte-identical to a no-xt call); with a fitted xt, L2 cols finite; unresolvable goal → honest-NaN; non-two-team frame → `pd.NA` numerical superiority. Conservation census.

- [ ] **Step 2–4:** Run-fail → writer (runs in the tracking-marts drain; consumes tracking frames + actions per unit; inject xt for L2) → run-pass.

- [ ] **Step 5:** `fct_rest_defense` mart (action-grid grain — new mart, NOT widening `fct_action_context`, per spec §4/§11 resolution: distinct concept, keep the AC contract clean). `_marts__models.yml` contract. restdefense is descriptive/team-structural — confirm NOT per-player-evaluative (no governance card; add the exclusion comment). `NOTICE` + Appendix D (Forcher 2023, Peters 2025, Memmert 2022, Dash 2025).

---

## Phase F — cross-cutting UI + finalization

### Task F1: Taipy pages (grain-bucketed)

**Files:** Create `hf_taipy_app/src/state/{team_kpis,gk_decision_shot_stopping,player_territory_duels,vaep_adjusted}.py` + `hf_taipy_app/src/pages/*.py`. Modify `hf_taipy_app/src/main.py` (PAGE_REGISTRY), `hf_taipy_app/src/template.py` (PAGE_TERMS + GLOSSARY).

- [ ] **Step 1:** One page per grain-bucket via `build_page(PageConfig)` — team KPIs; keeper (decision + shot-stopping, with the comparison affordance); player (territory + duels); VAEP-adjusted (side-by-side vs raw VAEP delta). Every metric shows scale + direction (0–1, higher/lower = better) in tooltip/caption; `Citation` per methodology; `help_text` on every Metric/StatCard; no raw IDs (join to dim tables); goal-oriented nav labels.
- [ ] **Step 2:** GLOSSARY + PAGE_TERMS entries for every new term; `NOTICE` entry per page methodology.
- [ ] **Step 3:** Bound every query (`LIMIT 500`/`2000`); page-load ≤3s. Run the Taipy pyright leg (`pyright hf_taipy_app/src/`).

### Task F2: P1 verification + commit gate

- [ ] **Step 1:** Full 7-gate suite + pip-audit green (as P0 Task 5 Step 1).
- [ ] **Step 2:** Governance + appendix + `_marts__models.yml` completeness + all metric parity/census tests green.
- [ ] **Step 3:** HF publisher stubs for any new public dataset/mart go through `ingestion.hf_upload_seam.prepare_public_upload` (ADR-072); `upload_hf_readme` with a card resolved via `get_hf_card_path`; `test_publisher_seam_conformance` + `test_hf_publish_parity` green.
- [ ] **Step 4: STOP — request explicit owner approval** before committing the P1 metric-family commits (one coherent commit per family, or per grain-bucket — owner's call). No push/PR (deferred to end-of-cycle).

---

## Self-Review

1. **Spec coverage:** §4 grain-bucketing → Phases A/B/C/D/E marts; §5.1 event-only → A/B1/C/D; §5.2 tracking → B2/E; §5.3 shot_stopping 3-mart supersession → B1; §5.4 (elastic) is P0; §7 governance/refs/UI → per-metric governance steps + Phase F. All 8 metrics have a writer+mart+wiring task. ✓
2. **Placeholders:** every task names exact files, the exact sk `compute_*` signature (from introspection), grain keys, and mart target; the invariant writer skeleton is cited to `bravery_writer.py` (a real template, not a placeholder). Column literals are pinned to the sk `*_METRIC_COLUMNS` constants via parity tests (the repo's schema-drift idiom). ✓
3. **Type consistency:** grain keys (`game_id`/`team_id`/`player_id`/keeper), the `_METRIC_COLUMNS` constant per package, and the native-id→surrogate mart-join convention are consistent across all phases. duels' single-driver ordered pass (not `applyInPandas`) and gk_decision/restdefense's tracking-drain placement are called out as deliberate deviations. ✓
