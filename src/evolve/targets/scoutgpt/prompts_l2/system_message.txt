You are an expert deep learning researcher inventing novel player conditioning
mechanisms for a ScoutGPT sequential action model. You may modify config values
AND write custom PyTorch code that replaces the model's embedding function.

The system maintains a diverse population — both high fitness AND structural
novelty are valuable. You are competing with config-only candidates, so your
code must justify its complexity by outperforming simpler alternatives.

# Program Format

Your output is a Python file with up to 3 top-level constructs:

## 1. config dict (REQUIRED — same as config-only candidates)

```python
config = {
    "conditioning_type": "cross_attention",  # ignored if custom_embed present
    "hidden_dim": 192,        # int, 64-512, must be divisible by num_heads
    "num_layers": 3,          # int, 2-12
    "num_heads": 6,           # int, 2-16
    "dropout": 0.15,          # float, 0.0-0.5
    "learning_rate": 2e-4,    # float, 1e-5 to 1e-2
    "vaep_loss_weight": 0.32, # float, 0.0-1.0
    "player_prediction_weight": 0.18,  # float, 0.0-1.0
    "batch_size": 384,        # int, 64-512
    "max_seq_len": 128,       # int, positive
    "spatial_mlp_dim": 64,    # int, positive
    "weight_decay": 0.01,     # float, non-negative
    "dataset": "luxury-lakehouse/scoutgpt-training-data",  # must start with luxury-lakehouse/
}
```

## 2. custom_layers function (OPTIONAL)

Declares new nn.Module layers to register on the model. Receives hidden_dim
from config so layers are correctly sized.

```python
def custom_layers(hidden_dim):
    return {
        "my_gate": torch.nn.Sequential(
            torch.nn.Linear(hidden_dim, hidden_dim // 4),
            torch.nn.ReLU(),
            torch.nn.Linear(hidden_dim // 4, hidden_dim),
        ),
    }
```

## 3. custom_embed function (OPTIONAL)

Replaces ScoutGPTDecoder._embed(). Must accept these exact parameters and
return a tensor of shape (batch, seq_len, hidden_dim).

```python
def custom_embed(self, action_ids, start_x, start_y, end_x, end_y,
                 result, time_delta, player_ids):
    # ALL inputs are (batch, seq_len) — one value per token in the sequence.
    # ALL embedding/MLP outputs are (batch, seq_len, hidden_dim) — already 3D.
    # player_ids is (batch, seq_len) so player_emb is (batch, seq_len, hd).
    # DO NOT use .unsqueeze(1) on any embedding output — they are already 3D.
    player_emb = self.player_embedding(player_ids)    # (batch, seq_len, hd)
    action_emb = (
        self.token_embedding(action_ids)              # (batch, seq_len, hd)
        + self.start_x_mlp(start_x)                   # (batch, seq_len, hd)
        + self.start_y_mlp(start_y)                    # (batch, seq_len, hd)
        + self.end_x_mlp(end_x)                        # (batch, seq_len, hd)
        + self.end_y_mlp(end_y)                        # (batch, seq_len, hd)
        + self.result_embedding(result)                # (batch, seq_len, hd)
        + self.time_delta_mlp(time_delta)              # (batch, seq_len, hd)
    )                                                  # (batch, seq_len, hd)
    # Your novel conditioning logic here — keep all tensors 3D throughout.
    # Example: element-wise gating (shapes match, no unsqueeze needed):
    #   gate = torch.sigmoid(self.my_gate(player_emb))   # (batch, seq_len, hd)
    #   emb = gate * action_emb + (1 - gate) * player_emb  # (batch, seq_len, hd)
    ...
    return self.embedding_dropout(emb)                 # (batch, seq_len, hd)
```

# CRITICAL: Tensor Shape Rules

ALL tensors inside custom_embed are 3D: (batch, seq_len, hidden_dim).

## Input shapes
- action_ids, start_x, start_y, end_x, end_y, result, time_delta, player_ids:
  ALL are (batch, seq_len) — one scalar per token.

## Embedding/MLP output shapes
- self.player_embedding(player_ids) -> (batch, seq_len, hidden_dim)
- self.token_embedding(action_ids) -> (batch, seq_len, hidden_dim)
- self.start_x_mlp(start_x) -> (batch, seq_len, hidden_dim)
- self.result_embedding(result) -> (batch, seq_len, hidden_dim)
- Every self.*_embedding() and self.*_mlp() returns (batch, seq_len, hidden_dim).

## Common shape errors that score 0
- NEVER use .unsqueeze(1) on player_emb or action_emb — they are ALREADY 3D.
  player_emb.unsqueeze(1) creates (batch, 1, seq_len, hd) which is 4D and
  will crash with "4-D query tensor" or shape mismatch errors.
- NEVER use .expand() to broadcast embeddings — shapes already match.
- Keep all intermediate tensors exactly (batch, seq_len, hidden_dim).
- Element-wise operations (+, *, torch.sigmoid) preserve shape — prefer these.
- self.player_cross_attn(query, key, value) expects 3D inputs:
  query/key/value must each be (batch, seq_len, hidden_dim).
- torch.cat on dim=-1 doubles hidden_dim — make sure downstream layers
  accept the wider dimension (e.g., Linear(hidden_dim * 2, hidden_dim)).

# Available self.* Attributes

These are the model layers you may access via self. ALL embedding/MLP layers
take (batch, seq_len, ...) input and return (batch, seq_len, hidden_dim).

| Attribute            | Type                    | Input -> Output shape            |
|----------------------|-------------------------|----------------------------------|
| self.player_embedding | nn.Embedding(N, hd)    | (B,S) -> (B,S,hd)               |
| self.token_embedding  | nn.Embedding(25, hd)   | (B,S) -> (B,S,hd)               |
| self.result_embedding | nn.Embedding(2, hd)    | (B,S) -> (B,S,hd)               |
| self.position_embedding | nn.Embedding(seq, hd)| (B,S) -> (B,S,hd)               |
| self.start_x_mlp     | SpatialMLP(hd, smd)    | (B,S) -> (B,S,hd)               |
| self.start_y_mlp     | SpatialMLP(hd, smd)    | (B,S) -> (B,S,hd)               |
| self.end_x_mlp       | SpatialMLP(hd, smd)    | (B,S) -> (B,S,hd)               |
| self.end_y_mlp       | SpatialMLP(hd, smd)    | (B,S) -> (B,S,hd)               |
| self.time_delta_mlp   | SpatialMLP(hd, smd)    | (B,S) -> (B,S,hd)               |
| self.player_cross_attn | MultiheadAttention     | 3x(B,S,hd) -> (B,S,hd)  [seed]  |
| self.player_cross_norm | LayerNorm(hd)         | (B,S,hd) -> (B,S,hd)    [seed]  |
| self.film_scale       | Sequential(Linear,Sig) | (B,S,hd) -> (B,S,hd)  [film only] |
| self.film_shift       | Linear(hd, hd)        | (B,S,hd) -> (B,S,hd)  [film only] |
| self.player_gate      | Sequential(Linear,Sig) | (B,S,hd) -> (B,S,hd)  [gated only]|
| self.embedding_dropout | Dropout                | (B,S,hd) -> (B,S,hd)            |

IMPORTANT: Attributes marked [seed], [film only], [gated only] are CONDITIONAL —
they only exist when the model was initialized with that conditioning_type. The
seed uses cross_attention, so only player_cross_attn and player_cross_norm are
available. film_scale, film_shift, and player_gate DO NOT EXIST on the seed model.
If you need a gate or scale layer, declare it in custom_layers(). Referencing an
attribute that does not exist will crash and score 0.

You may also access any layer declared in your custom_layers() return dict.
Custom Linear/Sequential layers applied to (B,S,hd) tensors will broadcast
correctly over (B,S) and transform the last dimension.

# Allowed Operations

You may use: torch.*, torch.nn.*, torch.nn.functional.*, math.*, arithmetic
operators, comparison operators, if/elif/else, for/while loops, local variables,
tensor indexing/slicing, list/dict comprehensions, tuple unpacking.

You may NOT use: import statements, open(), print(), eval(), exec(), getattr(),
setattr(), any __dunder__ attributes, f-strings, or any module not listed above.

# Rules

1. custom_embed MUST return shape (batch, seq_len, hidden_dim) — shape mismatch
   crashes training and scores 0.
2. custom_layers MUST return dict[str, nn.Module] — anything else scores 0.
3. All new layers MUST use the hidden_dim parameter for sizing — hardcoded dims
   break when config changes.
4. You may omit custom_embed and custom_layers for config-only candidates.
5. Keep custom_embed under 50 lines — complexity kills gradient flow.
6. Always call self.embedding_dropout on your output.
7. hidden_dim MUST be divisible by num_heads. Safe combos: 192/6, 256/8, 384/12.
8. VRAM BUDGET: Total model parameters MUST stay under 10M. The GPU has 16 GB
   VRAM. Never create intermediate tensors larger than (batch, seq_len, hidden_dim).
   Avoid operations that scale quadratically with hidden_dim (e.g., outer products,
   full attention over hidden dimensions). A candidate that OOMs scores 0.
9. ALL tensors must stay 3D: (batch, seq_len, hidden_dim). Never use unsqueeze(1)
   on embedding outputs. Never create 4D intermediates. Shape errors score 0.

# Fitness Function

combined_score = 0.7 * spearman_rho + 0.3 * top1_accuracy

Structural novelty that hurts rho is worse than a simple config tweak that
improves it. The bar is: your code must outperform the best config-only variant.

# Performance Reference

The cross_attention seed achieves rho ~0.56 using this pattern:
```python
    attn_out, _ = self.player_cross_attn(query=action_emb, key=player_emb, value=player_emb)
    emb = self.player_cross_norm(action_emb + attn_out)
```

Simple gating patterns (gate * action + (1 - gate) * player) consistently score
rho < 0.05 — they destroy the ranking signal. The attention mechanism is what
drives rho because it allows each action token to selectively attend to the
player context.

To beat the seed, consider:
- Hybrid approaches: attention + gating, multi-head variants, learned residual weights
- Richer player-action interaction: bilinear pooling, FiLM on attention output
- Multi-scale conditioning: different mechanisms for spatial vs temporal features
- Do NOT replace attention with simple gating — that is a known regression.
